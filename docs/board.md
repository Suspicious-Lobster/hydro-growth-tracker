# Hydro Growth Tracker -- Market-Ready Board

Machine-readable work board for the market-readiness pass (scoped 2026-09-02).
Every row carries a `<!--row ...-->` header that `python tools/board.py` reads;
status lives in the header, never in prose. `python tools/board.py lint` runs in
validate and fails on drift. `TASKS.md` remains the human feature backlog; rows
here are the fixes, hardening and tests that stand between the current build and
a product someone can pay for.

Evidence for every defect row is a probe that was actually run on 2026-09-02
against `createServer()` (see the row's Gap line). Nothing here is inferred from
reading alone unless the row says so.

Lanes: A = data safety and security (backend) . B = frontend correctness .
C = Electron shell and packaging . D = test infrastructure . E = docs, cleanup,
decisions.

Tiers: H = mechanical (Haiku) . S = spec-driven build (Sonnet) . O = the
foreman: data safety, security, gate design, shell wiring.

<!--row id=MR-24 tier=O status=done lane=D files=test/**,vitest.config.mjs,package.json,package-lock.json,test-server.mjs,test-migrate.mjs commit=e137530-->
**MR-24 [O] Backend tests onto vitest with unit coverage of validation and repository.** Gap: test-server.mjs (57 checks) and test-migrate.mjs (23) are hand-rolled pass/fail scripts sharing ONE data dir across every check, with no runner, no isolation, no unit tests for validation.js (0 direct tests of its 25 rules) or db/repository.js. Convert both into test/*.test.mjs under vitest at the repo root, one temp data dir per test, and add test/validation.test.mjs (every rule at its boundary: pH 0/14/14.1, EC 5/5.1, height 1000 cm vs 400 in, name 100/101 chars, date parse) and test/repository.test.mjs (pure entity helpers, prepareImport id rebuild, normalize). Add a c8 coverage gate on server.js, db/**, validation.js at 90 percent lines. Keep the check count from FALLING: 80 today is the floor (CODING-PRACTICES 1.6). npm test runs it; test:e2e is left alone for MR-19.
Accept: npm test runs vitest and reports at least 80 passing tests plus the new unit files; coverage summary prints and the run exits non-zero below 90 percent lines on the three backend paths.
Red proof: Change the EC upper bound in validation.js from 5 to 50 -> the boundary test fails. Delete the coverage threshold from the config -> the gate no longer fails when a route test is removed (the gate's own red).

<!--row id=MR-1 tier=O status=done lane=A deps=MR-24 files=db/repository.js,server.js,main.js,frontend/src/App.jsx,frontend/src/api/api.js,test/** commit=d957a3b-->
**MR-1 [O] A damaged data file must never be overwritten with an empty store.** Gap (probe P3, 2026-09-02): truncated hydro-data.json -> repo.load() logged an error and returned emptyData(); the next PUT /settings returned 200 and wrote a store with 0 plants over the user's file. 0 salvage copies. This is silent total data loss on the first write after any corruption. Fix: load() distinguishes MISSING (fresh install, empty store is right) from UNREADABLE (parse error). Unreadable -> copy the bytes to hydro-data.corrupt-<ts>.json, put the server into a damaged state: every write route returns 503 {error:'data file damaged', path}, GET / reports status 'damaged'. main.js shows a dialog naming the salvage path and the backups folder. The renderer shows a persistent banner (App.jsx) and disables mutations while GET / reports damaged.
Accept: Probe P3 re-run: PUT /settings on a truncated file returns 503, the data file bytes are unchanged, and exactly one .corrupt-<ts>.json copy exists whose bytes equal the original. A fresh install with NO file still starts with an empty store (the missing-vs-unreadable branch both ways, 1.9).
Red proof: Revert load() to returning emptyData() on parse error -> the test asserting 503 and unchanged bytes fails with plants 0. Separately, delete the missing-file branch -> the fresh-install test fails.

<!--row id=MR-2 tier=O status=done lane=A deps=MR-24 files=db/migrate.js,server.js,frontend/src/App.jsx,test/migrate.test.mjs,test/schema-version.test.mjs,test/helpers.mjs commit=de3715e-->
**MR-2 [O] Refuse data files and backups from a NEWER schema version.** Gap (probe P4, 2026-09-02): migrateData({schemaVersion:3,...}) treated the file as v1: changed=true, result schemaVersion 2, a plant's new_field dropped; POST /backup/restore of that v3 envelope returned 200 and wrote the stripped plants. A user who downgrades, or restores a backup from a later release, silently loses every field the newer version added. Fix: migrateData returns {error:'newer schema'} (or throws a typed error) for schemaVersion > SCHEMA_VERSION; runMigration leaves the file untouched and returns {migrated:false, tooNew:true}; createServer surfaces that as the same damaged state as MR-1 with a 'please update the app' message; restore returns 400 with a clear message.
Accept: migrateData on schemaVersion 3 returns an error and does not mutate its input; runMigration leaves the on-disk bytes identical; POST /backup/restore of a v3 envelope returns 400 and the store is unchanged (GET /plants before == after).
Red proof: Remove the version comparison -> the restore test reads 200 and the plants list changes; the migrate test sees changed=true.

<!--row id=MR-3 tier=O status=done lane=A deps=MR-24 files=db/repository.js,server.js,main.js,test/write-safety.test.mjs,test/helpers.mjs,test/server.test.mjs commit=fcce417-->
**MR-3 [O] Write safety: pre-restore snapshot, last-good backup on every save, Windows-safe rename.** Gap (probe P5, 2026-09-02): POST /backup/restore replaced the store with 0 pre-restore copies written; a mistaken restore is unrecoverable. save() does a bare writeFileSync + renameSync: no fsync, and on Windows renameSync fails with EPERM/EBUSY while an antivirus or indexer holds the target, which would surface as a 500 on an ordinary save. Fix: before restore write hydro-data.pre-restore-<ts>.json (keep the newest 5); on every successful save keep the previous file as hydro-data.json.bak (rolling, one copy); fsync the temp file before rename; retry rename up to 5 times with backoff on EPERM/EBUSY/EACCES; a daily auto-backup into <userData>/backups/hydro-data-<date>.json keeping 14 (TASKS.md item 18, pulled forward because it is the recovery path for MR-1).
Accept: After restore a pre-restore file exists whose bytes equal the previous store; after two saves .bak equals the first save's bytes; a save whose first rename throws EPERM (fs mocked once) succeeds and the file holds the new data; the backups dir gains one dated file per calendar day and never exceeds 14.
Red proof: Remove the snapshot call -> the byte-equality test fails; remove the retry -> the mocked-EPERM test throws; set the retention to 100 -> the 14-file cap test fails.

<!--row id=MR-4 tier=O status=done lane=A deps=MR-24 files=db/repository.js,server.js,validation.js,frontend/src/components/PlantManager.jsx,test/plant-names.test.mjs,test/helpers.mjs,test/server.test.mjs commit=37b7353-->
**MR-4 [O] Plant-name uniqueness across the archive boundary; restore endpoint with clash check.** Gap (probe P2, 2026-09-02): with an archived 'Tomato' present, POST /plants 'Tomato' -> 201 (two plants share the name), then POST /logs by plant_name 'Tomato' attached to the ARCHIVED plant (id 1, not the active id 2), and PUT restore of the archived one -> 200, leaving TWO ACTIVE plants named 'Tomato'. findPlantByName returns the first match regardless of archived state. Rule: a name is unique among ACTIVE plants. findPlantByName(data, name, {activeOnly}) prefers the active match; resolvePlant (logs and schedules by name) resolves to the active plant, never an archived one, and creates a new plant only when no active one exists. New POST /plants/:id/restore returns 409 if an active plant already has the name. PUT /plants/:id no longer requires name (partial update); validation checks name only when present. PlantManager's restore switches to the new endpoint.
Accept: Probe P2 re-run: creating 'Tomato' beside an archived 'Tomato' -> 201; a log posted by name attaches to the ACTIVE id; restoring the archived one -> 409 and the active count stays 1; renaming the archived one first, then restoring -> 200.
Red proof: Revert findPlantByName to first-match -> the log-by-name test reads the archived id; remove the restore clash check -> the 409 test reads 200 and active count 2.

<!--row id=MR-5 tier=O status=done lane=A deps=MR-24 files=server.js,main.js,preload.js,frontend/src/api/api.js,test/** commit=7fb2e5f-->
**MR-5 [O] Lock and relocate the local API: loopback only, random port, per-launch token, no wildcard CORS.** Gap (probe P1, 2026-09-02): GET /plants with Origin https://evil.example returned access-control-allow-origin: https://evil.example and allow-credentials: true, and there is no authentication. Any web page open in the user's browser can read, alter or wipe their grow data on http://localhost:5000. Port 5000 is also fixed: on macOS 12+ the AirPlay Receiver listens on 5000, so the app fails to start there. Fix: listen on 127.0.0.1 port 0; main.js mints a random 32-byte token per launch and passes {apiBase, token} to the renderer through preload.js via contextBridge (window.hydro); server middleware requires X-Hydro-Token on every route except GET / and /uploads/*; CORS allows only the dev origin http://localhost:5173 (and only when NODE_ENV=development); api.js reads window.hydro, falling back to VITE_API_BASE_URL plus VITE_API_TOKEN in dev. resolveImageUrl uses the same base. Dev script passes the token to Vite via env.
Accept: Request without the token -> 401 on /plants, /logs, /feeding, /settings, /backup; with the token -> 200; a request carrying Origin https://evil.example gets NO access-control-allow-origin header; two createServer instances on port 0 get different ports; server.address().address is 127.0.0.1. The Playwright smoke (MR-19) still loads data end to end through preload.
Red proof: Delete the token middleware -> the 401 test reads 200. Re-enable origin:true -> the evil-origin header test finds the reflected origin.

<!--row id=MR-6 tier=O status=done lane=C deps=MR-5 files=main.js,lifecycle.js,test/lifecycle.test.mjs,e2e/** commit=6a349f1-->
**MR-6 [O] Single-instance lock and correct quit lifecycle.** Gap (read from main.js, not probed): no app.requestSingleInstanceLock(), so two launches run two servers writing the same hydro-data.json -- last writer wins, silently. backendServer.close() runs on window-all-closed, but on macOS the app stays alive and 'activate' recreates a window whose renderer now has no backend. Fix: request the lock, quit the second instance and focus the first's window via second-instance; close the server on before-quit only; activate recreates the window against the still-running server.
Accept: Playwright: launching a second _electron instance against the same userData exits without opening a window and the first window is focused; closing the only window on Windows quits the app; a unit-level test of the lifecycle wiring (extracted into a small createLifecycle(app, startServer) helper) shows the server close is registered on before-quit and NOT on window-all-closed.
Red proof: Remove requestSingleInstanceLock -> the second launch opens a window and the e2e fails; move close() back to window-all-closed -> the wiring test fails.

<!--row id=MR-7 tier=S status=done lane=A deps=MR-24 files=server.js,db/repository.js,test/** commit=e21d27d-->
**MR-7 [S] Uploads: remove files when their log or plant is deleted; sniff image bytes and force the extension.** Gap (probe P6, 2026-09-02): a file named evil.html sent with mimetype image/png was stored as /uploads/<ts>-evil.html and is served by express.static from the API origin; deleting the log left the file on disk (uploads before/after delete: 1/1). Disk grows forever and the extension comes from the client. Fix: after multer stores the file, read the first 12 bytes; accept only PNG/JPEG/GIF/WebP magic numbers, rename to <random>.<ext-from-magic>, otherwise unlink and 400. deleteLog and deletePlantCascade return the image_url(s) removed; the route unlinks them (errors logged, never fatal). Make handle() await async handlers so the unlink is inside the try.
Accept: Probe P6 re-run: evil.html with PNG bytes is stored as .png; a PNG-mimetype upload with HTML bytes -> 400 and nothing on disk; deleting a log with an image leaves the uploads dir one file smaller; deleting a plant removes every image of its logs.
Red proof: Remove the unlink -> the before/after count test reads equal; remove the magic check -> the HTML-bytes upload test reads 201.

<!--row id=MR-8 tier=S status=done lane=A deps=MR-24 files=server.js,test/** commit=e21d27d-->
**MR-8 [S] CSV export: neutralise formula cells and write a UTF-8 BOM.** Gap (probe P7, 2026-09-02): a nutrients value of =HYPERLINK("http://evil","click") was written raw into the CSV; Excel and Sheets evaluate it on open (CSV injection). Non-ASCII notes also open garbled in Excel without a BOM. Fix: a csvSafe(value) helper prefixes a single quote to any string cell starting with =, +, -, @, tab or CR; export writes \uFEFF first; the same helper is unit-tested on its own.
Accept: Probe P7 re-run: the exported cell starts with '=HYPERLINK (quote-prefixed); a plain numeric string -5 is also prefixed; the response body's first code point is U+FEFF; a note with an accented character round-trips byte-exact after the BOM.
Red proof: Remove the prefix for '=' only -> the =HYPERLINK assertion fails while the -5 one passes (1.5b: one assertion per trigger character).

<!--row id=MR-9 tier=H status=done lane=A deps=MR-24 files=server.js,db/repository.js,test/** commit=e21d27d-->
**MR-9 [H] Remove the deprecated delete-logs-by-plant-name route.** Gap: DELETE /logs/plant/:plantName and repo.deleteLogsByPlantName delete every log whose plant_name matches -- across an archived plant and an active one that share a name (see MR-4). grep -rn "logs/plant" frontend/src returns nothing: no client calls it. Remove the route, the helper, and the test that exercised it; add a test that the path now 404s.
Accept: grep -rn deleteLogsByPlantName . --include=*.js --include=*.mjs (excluding node_modules) returns zero hits; DELETE /logs/plant/Tomato returns 404 and the log count is unchanged.
Red proof: Re-add the route -> the 404 test reads 200.

<!--row id=MR-10 tier=S status=done lane=B deps=MR-24 files=db/repository.js,frontend/src/components/LogViewer.jsx,frontend/src/components/PlantDetail.jsx,test/** commit=e21d27d-->
**MR-10 [S] Order and display logs by the measurement date the user entered.** Gap (probe P8, 2026-09-02): GET /logs returned a log dated 2020-01-01 FIRST because listLogs sorts by created_at (insert time); LogViewer and the PlantDetail history table both render formatDate(log.created_at), so a backdated entry shows today's date. Meanwhile utils/stats sorts by date. Two orderings for one list (CODING-PRACTICES 5.4). Fix: listLogs sorts by date desc, then created_at desc, with a deterministic fallback when either is missing (a log with no created_at made the comparator NaN in probe P8's first run); LogViewer and PlantDetail show log.date, with created_at only as a secondary 'logged <datetime>' caption.
Accept: A backdated log lists last in GET /logs and its row shows 2020 in both views (component test asserts the rendered date text, 1.2); a log with created_at missing sorts without throwing and never reorders between two calls.
Red proof: Revert the sort key to created_at -> the ordering test fails; revert the JSX to created_at -> the rendered-text test fails.

<!--row id=MR-11 tier=O status=done lane=B files=frontend/src/utils/format.js,frontend/src/utils/dates.js,frontend/src/components/AddLogForm.jsx,frontend/src/components/FeedingScheduleCalendarExport.jsx,frontend/src/utils/stats.js,frontend/src/utils/trends.js,frontend/src/__tests__/format.test.js,frontend/src/__tests__/dates.test.js,frontend/src/__tests__/trends.test.js,frontend/src/__tests__/stats.test.js,.github/workflows/ci.yml commit=a80b8a8-->
**MR-11 [O] Calendar dates are local dates everywhere: form default, display, sorting, streaks.** Gap (measured 2026-09-02 with explicit offsets, because Node on Windows ignores TZ): formatDate('2026-06-26') renders 'Jun 25, 2026' for a user in America/Los_Angeles (date-only strings parse as UTC midnight, then render in local time); AddLogForm's default date is new Date().toISOString().slice(0,10), which at 8pm in Los Angeles yields tomorrow and on THIS machine (Africa/Johannesburg) before 2am yields yesterday. careStreak buckets by UTC day. Locale is hard-coded en-US. Fix: one utils/dates.js with parseLocalDate('YYYY-MM-DD') (components, not Date.parse), todayLocalISO(now), and dayKey(log) used by format, stats, trends and the calendar export; formatDate uses the user's locale (undefined) and parseLocalDate for date-only input, Date for full timestamps. CI runs the frontend unit tests under TZ=America/Los_Angeles AND TZ=Pacific/Auckland on ubuntu (where TZ is honoured).
Accept: Unit tests: formatDate('2026-06-26') contains '26' under both CI zones; todayLocalISO(new Date(2026,5,26,20)) === '2026-06-26' and todayLocalISO(new Date(2026,5,26,1)) === '2026-06-26' (built with local components so the assertion is zone-independent by construction, and the CI zones prove the negative-offset case); careStreak counts a 23:30 log and the next day's 00:30 log as two days.
Red proof: Revert formatDate to new Date(string) -> the TZ=America/Los_Angeles CI job fails on '25'; revert the default date to toISOString -> the 8pm test fails on '27' in the LA job.

<!--row id=MR-12 tier=S status=done lane=B deps=MR-10 files=frontend/src/components/LogViewer.jsx,frontend/src/components/FeedingSchedule.jsx,frontend/src/components/ui/ConfirmDialog.jsx commit=5b8561d-->
**MR-12 [S] Log editing picks the plant from a selector, edits every field, and confirms deletes in-app.** Gap (read from LogViewer.jsx): the edit form sends plant_name as free text; the server's resolvePlant then CREATES a new plant for any typo, so 'fixing' a name silently forks the plant. The edit form omits date, PPM, temps, humidity, light, reservoir and stage (users cannot correct them). Deletes use window.confirm, which blocks the renderer and cannot be styled or tested; every other destructive action already uses Modal. Fix: plant chosen by plant_id from useAppData().plants; all measurement fields editable in the active display units (reuse AddLogForm's conversion helpers); PUT sends plant_id never plant_name; a ConfirmDialog built on Modal replaces window.confirm here and in FeedingSchedule.
Accept: Component test: editing a log and saving calls updateLog with {plant_id: <number>} and no plant_name key, and the store's plant count is unchanged; editing the date field sends date; clicking delete renders the ConfirmDialog and no window.confirm is called (spy asserts 0 calls).
Red proof: Send plant_name again -> the payload test fails; call window.confirm -> the spy count reads 1.

<!--row id=MR-13 tier=S status=done lane=B files=frontend/src/contexts/AppDataContext.jsx,frontend/src/App.jsx,frontend/src/__tests__/components/**,frontend/package.json,frontend/package-lock.json,frontend/vitest.config.js,frontend/src/test-setup.js commit=4e03c77-->
**MR-13 [S] Mutations refresh in the background instead of unmounting the current view.** Gap (read from AppDataContext.jsx and App.jsx): loadAll dispatches LOAD_START on every mutation, App renders a spinner while loading, so every save unmounts the visible view and wipes its local state (PlantManager's archived list, LogViewer's scroll, Bud's position). Fix: loading is true only until the first successful load; later refreshes set refreshing (used for a subtle top bar), and a failed refresh keeps the last good data with the error banner.
Accept: Component test: mount a view with local state, trigger a mutation, and assert the same element instance is still mounted with its state intact after the refetch resolves; the initial load still shows the spinner.
Red proof: Dispatch LOAD_START from the refresh path -> the local state assertion fails.

<!--row id=MR-33 tier=S status=done lane=B deps=MR-11 files=validation.js,frontend/src/components/AddLogForm.jsx,frontend/vite.config.js,frontend/src/__tests__/validation-shared.test.js commit=3aa0990-->
**MR-33 [S] One set of validation rules: the frontend imports the server's validation.js.** Gap (read): AddLogForm.validate() re-implements three of the server's 25 rules and none of the ranges, so a pH of 15 passes the client and fails on the server with a toast. CODING-PRACTICES 5.4: one derivation per fact. Fix: validation.js exports the rule table (already pure ESM, no Node imports); vite.config.js allows the import from the repo root (server.fs.allow or an alias); AddLogForm runs validateLog on the canonical payload before posting and maps each message to its field.
Accept: Entering pH 15 shows the server's exact message inline without a request (axios mock records 0 calls); a valid form posts once. A test imports the same function from both paths and asserts identity.
Red proof: Duplicate the pH rule locally with a different bound -> the identity test fails and the inline message differs from the server's text.

<!--row id=MR-14 tier=S status=done lane=C files=assets/**,scripts/make-icons.mjs,package.json,main.js commit=37178ce-->
**MR-14 [S] Real application icons for Windows, macOS and Linux.** Gap (measured 2026-09-02): assets/icons/icon.png is a 98-byte TEXT file holding the base64 of a 1x1 PNG; `file` reports ASCII text. package.json points mac and linux icons at an SVG, which electron-builder cannot use, and sets no win icon, so the installer and taskbar show Electron's default. Fix: scripts/make-icons.mjs rasterises assets/icon.svg with sharp to 1024 PNG and derives icon.ico (16-256) and icon.icns; package.json build.win/mac/linux point at them; BrowserWindow gets icon on Linux; a repo test asserts the PNG magic bytes and 1024x1024 dimensions and that every icon path in package.json exists.
Accept: test/assets.test.mjs: icon.png starts with 89 50 4E 47 and its IHDR reads 1024x1024; icon.ico and icon.icns exist and are over 10 KB; every build.*.icon path resolves. electron-builder --dir completes with no 'default Electron icon is used' warning in its log.
Red proof: Replace icon.png with the current text file -> the magic-bytes test fails; point build.win.icon at a missing path -> the path test fails.

<!--row id=MR-15 tier=S status=done lane=C deps=MR-5 files=frontend/index.html,frontend/vite.config.js,main.js,frontend/src/components/AboutDialog.jsx,frontend/src/components/SettingsPanel.jsx,frontend/src/__tests__/components/AboutDialog.test.jsx,e2e/** commit=e1374f4-->
**MR-15 [S] Window title, Content-Security-Policy, production menu and About dialog.** Gap (read): frontend/index.html's title is 'Vite + React' and its favicon is vite.svg; there is no CSP meta so Electron prints its insecure-CSP warning and any injected script would run; the default menu exposes Toggle Developer Tools in production; nothing shows the version. Fix: title 'Hydro Growth Tracker'; CSP meta: default-src 'self'; connect-src 'self' <apiBase from MR-5 via preload>; img-src 'self' data: blob: <apiBase>; style-src 'self' 'unsafe-inline' (Tailwind) ; a minimal application menu (File: Backup..., Quit; Help: About) in production, full menu in dev; About reads app.getVersion() through preload.
Accept: Playwright: window title equals 'Hydro Growth Tracker'; a CSP meta element exists and the console shows no 'Insecure Content-Security-Policy' warning; Help > About shows the package.json version string.
Red proof: Delete the CSP meta -> the console-warning assertion fails; change the title -> the title assertion fails.

<!--row id=MR-17 tier=S status=done lane=E files=assets/installer.nsh,assets/installer-pro.nsh,start.bat,start.ps1,package.bat,setup.js,convert-icons.bat,scripts/pre-build.js,package.json,README.md,test/repo-hygiene.test.mjs commit=eaccf18-->
**MR-17 [S] Remove dead installer scripts and dev-launcher cruft; make package.json describe what is built.** Gap (read): assets/installer*.nsh are not referenced by package.json's nsis block (no include key) and reference header.bmp/wizard.bmp that do not exist; installer.nsh creates $INSTDIR\database which the app never uses. scripts/pre-build.js regenerates start.bat/start.ps1 as 'production files' although they only run npm run dev. package.bat, setup.js and convert-icons.bat are unreferenced. README promises a Windows portable build the config does not produce. Fix: delete the listed files; drop the pre-build step from dist; add the portable target (or drop the README claim -- recommend adding it); a repo-hygiene test asserts every path named in package.json scripts and build.files exists and no *.bat/*.ps1 remain at the root.
Accept: npm run dist -- --dir produces dist/win-unpacked; the hygiene test passes; git ls-files shows none of the deleted paths.
Red proof: Reference a missing file from build.files -> the hygiene test fails; re-add start.bat -> the no-root-launchers assertion fails.

<!--row id=MR-18 tier=H status=done lane=E flags= files=package.json,README.md,frontend/index.html,test/repo-hygiene.test.mjs commit=aec1655-->
**MR-18 [H] One product name everywhere.** Gap (read): package.json productName is 'Hydro Growth Tracker' while nsis.shortcutName is 'Hydro Growth Tracker Pro'; installer-pro.nsh says 'HydroGrowth Tracker Pro'; the window says 'Vite + React'. OWNER decision: which name ships (recommend 'Hydro Growth Tracker', dropping 'Pro' until there is a non-Pro edition to contrast it with). Then a single grep-driven pass makes every surface agree.
Accept: grep -rn -i "tracker pro\|HydroGrowth\|Vite + React" over tracked files (excluding this board) returns zero hits; the installed shortcut, window title and About dialog all read the chosen name.
Red proof: Re-introduce 'Pro' in shortcutName -> the grep test (test/repo-hygiene.test.mjs names the forbidden strings) fails.

<!--row id=MR-19 tier=O status=done lane=D deps=MR-5 flags=tree files=e2e/**,playwright.config.mjs,package.json,package-lock.json,test-e2e.cjs,main.js,README.md,.gitignore commit=1d66a19-->
**MR-19 [O] Playwright end-to-end suite that drives the real Electron app.** Gap: test-e2e.cjs is not a test -- it greps source files for strings like 'localStorage' and 'isDirty' and runs npm install as a step; it passes with every feature deleted so long as the words remain (CODING-PRACTICES 3.4b, a gate watching a filing cabinet). Nothing launches the app. Fix: @playwright/test with _electron.launch against main.js and a temp userData; specs: first-run empty state; create plant; add log with a real PNG and see it on the dashboard with its photo; edit a log; export CSV and read the file; backup then wipe then restore and see the counts return; archive and delete a plant; settings unit change re-renders heights in inches. Delete test-e2e.cjs; test:e2e runs Playwright. flags=tree: it owns the screen.
Accept: npm run test:e2e passes locally with at least 8 specs, each asserting rendered text or a file on disk, never the request payload alone; the run writes a trace on failure.
Red proof: Break POST /logs to return 500 -> the add-log spec fails at 'log appears on dashboard'; the suite must be seen red this way before it is trusted.

<!--row id=MR-20 tier=O status=done lane=D deps=MR-19,MR-14,MR-11 files=.github/workflows/ci.yml commit=d61c635-->
**MR-20 [O] CI: packaging smoke on Windows, e2e, and a timezone matrix.** Gap: ci.yml runs lint, build, the two backend scripts and frontend unit tests on ubuntu only; nothing packages the app or launches it, and the TZ bugs of MR-11 are invisible on a UTC runner. Fix: job 1 (ubuntu) lint + unit + backend, run twice under TZ=America/Los_Angeles and TZ=Pacific/Auckland; job 2 (windows-latest) npm run dist -- --dir then the Playwright e2e against the unpacked build, uploading the trace and the unpacked dir as artefacts; concurrency cancel-in-progress; Node 20 pinned.
Accept: A push to the PR branch shows both jobs green; the artefact list contains win-unpacked; deliberately breaking main.js in a scratch commit turns job 2 red (then revert).
Red proof: The scratch-commit break IS the red proof: job 2 must be observed red once before the badge is believed.

<!--row id=MR-21 tier=O status=closed lane=C deps=MR-20 flags=owner files=package.json,.github/workflows/release.yml,main.js,docs/release.md-->
**MR-21 [O] Code signing, notarisation and auto-update.** Gap: no signing config, so Windows SmartScreen shows 'unknown publisher' and macOS Gatekeeper refuses to open the app; no update channel, so every fix needs a manual reinstall. OWNER decision: buying an OV/EV code-signing certificate (Windows) and an Apple Developer membership (macOS) is a purchase; hosting releases on GitHub Releases is a channel choice. Recommend: GitHub Releases + electron-updater; sign Windows via CSC_LINK secrets; notarise mac via notarytool in CI.
Accept: signtool verify /pa passes on the built installer; spctl --assess passes on the mac build; a test against a mocked update feed shows the app detects a newer version and downloads it; docs/release.md walks a release end to end.
Red proof: Build without the secrets -> signtool verify fails and the release workflow refuses to publish (the workflow asserts the signature before upload).

<!--row id=MR-22 tier=S status=done lane=E files=LICENSE,README.md,docs/user-guide.md,test/repo-hygiene.test.mjs commit=4536e86-->
**MR-22 [S] LICENSE file, privacy statement and a user guide that says where the data lives.** Gap: README and package.json claim MIT but there is no LICENSE file (ls LICENSE* -> none), so the licence is unenforceable and GitHub shows none; nothing tells a user that all data is local with no telemetry, where hydro-data.json and uploads live per OS, or how to back up before an uninstall. Fix: MIT LICENSE with the author; README 'Privacy and your data' section; docs/user-guide.md covering first run, backup/restore, where data lives (%APPDATA%, ~/Library/Application Support, ~/.config), and recovery from the .corrupt/.bak files of MR-1 and MR-3.
Accept: The hygiene test asserts LICENSE exists, contains 'MIT License', and package.json license equals MIT; README contains a 'Privacy' heading; every npm script named in README exists in package.json.
Red proof: Delete LICENSE -> the test fails; name a fake npm script in README -> the script-existence assertion fails.

<!--row id=MR-23 tier=S status=done lane=C deps=MR-5 files=main.js,server.js,frontend/src/components/ErrorBoundary.jsx,test/** commit=13cbb42-->
**MR-23 [S] Errors go to a log file the user can find, and the error screen can copy them.** Gap: console.error is the only sink; in the packaged app there is no console, so a failed migration, a damaged store or a route throw vanish. The ErrorBoundary hides details outside dev and offers no way to report. Fix: a tiny logger (append-only, <userData>/logs/hydro.log, rotated at 1 MB) injected into createServer and used by main.js; unhandled main-process errors show a dialog naming the log path; ErrorBoundary shows a 'Copy details' button (error, component stack, app version) in production too.
Accept: Throwing inside a route in a test writes a line containing the route and message to the log path; the dialog path is unit-tested through the extracted lifecycle helper of MR-6; the boundary's copy button writes to the clipboard mock.
Red proof: Remove the logger injection -> the log file is absent and the test fails.

<!--row id=MR-25 tier=S status=done lane=D deps=MR-12,MR-13,MR-33 files=frontend/src/__tests__/components/**,frontend/package.json,frontend/vitest.config.js,frontend/src/test-setup.js commit=b3116bc-->
**MR-25 [S] Component tests for every form and destructive flow.** Gap: 93 vitest tests cover only pure utils; not one component is rendered in a test, so unit conversion in AddLogForm (inches to cm), draft save/restore, validation messages, the restore-confirm modal and settings save are all unverified except by hand. Fix: jsdom environment, @testing-library/react and user-event, axios mocked via vi.mock; tests: AddLogForm converts 10 in to 25.4 cm in the payload and restores a draft; LogViewer edit (MR-12) ; PlantManager create/archive/restore; BackupRestore shows counts and posts only after confirm; SettingsPanel saves and re-renders a height in inches; Dashboard shows an out-of-range alert for pH 7.5 on a tomato. Every assertion is on rendered output or the recorded request (CODING-PRACTICES 1.2).
Accept: npm run test:unit reports at least 25 component tests passing in addition to the existing 93; total count is printed and the floor asserted in CI (1.6).
Red proof: Flip toCm to return v unchanged -> the AddLogForm payload test fails on 10 vs 25.4; remove the confirm step in BackupRestore -> the 'posts only after confirm' test fails.

<!--row id=MR-26 tier=S status=done lane=D files=frontend/src/__tests__/plantKnowledge.test.js commit=69a86a4-->
**MR-26 [S] Invariant tests over the plant knowledge base and feeding programme.** Gap: plantKnowledge.js (632 lines of hand-typed ranges) and feedingSchedule.js drive every recommendation and alert, and nothing checks them: a swapped min/max or a gap between stage height ranges would misclassify every reading silently. Fix: for every species: phRange, optimalTemp, optimalHumidity present with min < max; stages appear in GROWTH_STAGES order; heightRanges are contiguous and non-overlapping; every ec range min < max; every FEEDING_SCHEDULE week names a stage that stageToWeek can map back. The checker is a function tested against the real table AND against a deliberately corrupted copy so its own red is proven (1.1f).
Accept: The test prints species x invariants checked (expected 10 species) and passes on the real table; the corrupted-copy test asserts the checker returns the exact violation.
Red proof: Swap tomato seedling ec min/max in a fixture copy -> the checker names 'tomato.seedling.ec'; if it does not, the checker is the defect.

<!--row id=MR-27 tier=H status=done lane=D files=frontend/src/contexts/ThemeContext.jsx,frontend/package.json commit=56451c5-->
**MR-27 [H] Lint at zero warnings.** Gap: lint runs with --max-warnings 1 -- a ceiling set to exactly the one warning the tree has (CODING-PRACTICES 1.7c: a floor set from the shipped file certifies the file, not the property). The warning is react-refresh/only-export-components on ThemeContext's useTheme export; the other two contexts already carry the documented disable comment. Fix: add the same eslint-disable-next-line with its reason to ThemeContext, set --max-warnings 0.
Accept: npm --prefix frontend run lint exits 0 and prints 0 problems.
Red proof: Remove the disable comment -> lint exits non-zero.

<!--row id=MR-28 tier=S status=done lane=B deps=MR-12,MR-13,MR-19 files=frontend/src/components/**,frontend/src/__tests__/components/Modal.test.jsx,frontend/tailwind.config.js,e2e/** commit=6ff23d6-->
**MR-28 [S] Accessibility floor: labelled controls, focus-trapped dialogs, zero serious axe findings.** Gap (read): icon-only buttons rely on title alone in several places; Modal closes on Escape but does not trap or restore focus; nothing measures accessibility. Fix: aria-label on every icon-only button; Modal traps Tab within itself and returns focus to the opener on close; an axe-core pass in the Playwright suite on each tab.
Accept: The e2e a11y spec reports 0 serious or critical axe violations on the six tabs; a component test opens a Modal, presses Tab from its last control and asserts focus wraps to the first.
Red proof: Remove one aria-label -> axe reports a button-name violation and the spec fails; remove the trap -> the wrap test fails.

<!--row id=MR-29 tier=S status=done lane=E deps=MR-24 files=README.md,TASKS.md,docs/**,test/repo-hygiene.test.mjs commit=45540b3-->
**MR-29 [S] Docs match the build: README, TASKS pointer, architecture notes for the token and port.** Gap: README documents the fixed port 5000 architecture, a portable build that is not configured, and a test:e2e that is the string-grep script; TASKS.md does not point at the machine board. Fix: README reflects MR-5, MR-17, MR-19; TASKS.md gains a line pointing at docs/board.md and board.py; the hygiene test cross-checks README's npm scripts and the ports/paths it names against package.json and main.js.
Accept: The hygiene test passes; README no longer contains 'localhost:5000'.
Red proof: Put 'localhost:5000' back in README -> the hygiene test fails.

<!--row id=MR-30 tier=H status=done lane=E deps=MR-20 files=package.json,frontend/package.json,CHANGELOG.md,test/repo-hygiene.test.mjs commit=6152151-->
**MR-30 [H] Version 1.2.0 and a CHANGELOG.** Gap: no CHANGELOG exists; root and frontend package.json both say 1.1.0 and nothing keeps them equal. Fix: bump both to 1.2.0, write CHANGELOG.md from the board's done rows (one line per row, grouped by lane), and assert version equality in the hygiene test.
Accept: The hygiene test asserts root and frontend versions are equal and CHANGELOG.md's first heading names that version.
Red proof: Bump only the root version -> the equality test fails.

<!--row id=MR-31 tier=O status=closed lane=E flags=owner files=docs/decisions.md-->
**MR-31 [O] Owner ruling: distribution channel, and whether the cannabis profile ships.** Gap: PLANT_TYPES includes cannabis with a full profile. Direct download has no policy issue; the Microsoft Store and Apple App Store restrict cannabis-related apps, and some jurisdictions restrict cultivation guidance. This is a product and legal decision, not a code decision. Options: (a) direct download only, keep the profile (recommend: simplest, widest); (b) stores too, ship the profile behind an opt-in with a jurisdiction note; (c) drop the profile. Record the ruling in docs/decisions.md; MR-21 consumes it.
Accept: docs/decisions.md contains a dated ruling naming the channel and the profile decision, signed by the owner; the board's MR-21 body references it.
Red proof: A decision row has no code to break; it closes on the ruling. The red proof is that MR-21 cannot start until the file exists (its Gap line asserts the path).

<!--row id=MR-16 tier=O status=done lane=C deps=MR-19 flags= files=package.json,package-lock.json,main.js,preload.js commit=aab5970-->
**MR-16 [O] Upgrade Electron 29 to a currently supported major (and electron-builder to match).** Gap: node_modules/electron is 29.4.6 (2024); Electron supports the latest three majors only, so 29 receives no Chromium security fixes. A product shipping a two-year-old browser engine is not market ready. OWNER decision because a major upgrade can change preload, sandbox and builder behaviour and needs a real launch check on each platform the owner intends to ship. Depends on the e2e harness so the oracle exists before the bump.
Accept: npm ls electron shows a supported major; the Playwright e2e suite is green against the upgraded build; npm audit reports no high or critical advisory attributable to electron or electron-builder.
Red proof: The gate is the e2e suite: before bumping, break the preload path in main.js and confirm the suite fails on the first data load; then bump and watch it pass.

<!--row id=MR-34 tier=S status=done lane=B files=frontend/src/data/plantKnowledge.js,frontend/src/data/recommendations.js,frontend/src/components/PlantManager.jsx,frontend/src/components/PlantCards.jsx,frontend/src/__tests__/plantKnowledge.test.js,frontend/src/__tests__/recommendations.test.js,frontend/src/__tests__/components/PlantCards.test.jsx commit=a0403f4-->
**MR-34 [S] Seven species in the picker have no profile and silently get generic guidance.** Gap (found by MR-26, 2026-09-02): PLANT_TYPES lists 10 species and the species picker offers all 10, but PLANT_PROFILES defines only tomato, lettuce and generic. Basil, pepper, cucumber, strawberry, spinach, kale and cannabis fall back to the generic profile through getProfile(), so a basil plant shows tomato-agnostic generic pH/EC/temperature targets while the card says Basil. Fix: author real profiles for the seven (phRange, optimalTemp, optimalHumidity, stage heightRanges contiguous, ec per stage, feeding and care text) from standard hydroponic references, and until a profile exists the UI must say 'generic guidance' beside the stage label instead of implying species-specific targets. MR-26's checker already validates any profile added.
Accept: plantKnowledge.test.js reports 10 species x invariants with zero violations; recommendations.test.js asserts getProfile('basil') is not the generic profile; a component test asserts a plant whose species lacks a profile renders the text 'generic guidance'.
Red proof: Delete the basil profile -> the 10-species assertion and the getProfile test fail; remove the fallback label -> the component test fails.

<!--row id=MR-35 tier=S status=done lane=E files=package.json,frontend/package.json,.nvmrc,test/repo-hygiene.test.mjs,README.md commit=4319216-->
**MR-35 [S] Node 22 baseline: engines field, .nvmrc, and a check that CI matches.** Gap (retrospective, mr-run1): three separate incidents in one run were the same pattern, the toolchain quietly assuming Node 22 while the machine runs 20.14: root vitest pulled vite 8 and the bare 'cd frontend && vite' script resolved it (build broken); Electron 44's installer is ESM-only and would not run; electron-builder 26 exits 1 at its blockmap step. Each cost a diagnosis. Fix: engines.node '>=22.12' in both package.json files, an .nvmrc, README already says 22; the hygiene test asserts ci.yml's node-version satisfies engines and that engines is present. Owner action: install Node 22 on this machine, after which electron-builder can move back to 26.
Accept: hygiene test fails if engines.node is missing or ci.yml pins a lower major than engines requires; npm install prints an EBADENGINE warning on Node 20.
Red proof: Set ci.yml node-version to 20 -> the hygiene assertion fails.

<!--row id=MR-36 tier=S status=done lane=D files=tools/board.py,tools/test_board.py,board.config.json commit=1a8482c-->
**MR-36 [S] validate ladder that knows which test files belong to in-flight rows.** Gap (retrospective, mr-run1): four times the full validate ladder was red only because a worker's in-flight test file (MR-34's ten-species assertion, MR-23's kaboom boundary test, MR-25's require in a test, Modal.test) was mid-edit while the foreman validated a finished row; each time the foreman re-ran with a hand-typed --exclude list. Fix: board.py gains 'validate' which reads open doing rows' footprints and emits the vitest --exclude globs for files under them, so the exclusion is derived from the board, never typed.
Accept: python tools/board.py validate --exclude-in-flight prints one --exclude per test file inside a status=doing footprint and nothing for done rows; test_board.py covers both.
Red proof: Mark a row doing whose footprint holds a test file and assert the flag appears; set it done and assert it disappears.

<!--row id=MR-37 tier=O status=done lane=F files=server.js,db/repository.js,validation.js,test/** commit=f5b1e7c-->
**MR-37 [O] Backend: structured nutrient doses on logs, reservoir-event entity, optional nutrients text, photo replace on edit (TASKS 1, 2, 9, 13 backend half)** Gap: grep -rn 'doses\|reservoir_event' server.js db validation.js frontend/src returns nothing; validation.js line 'Nutrients information is required' blocks a quick log (TASKS 9); PUT /logs/:id has no multer so a photo can never be added to an existing log. Design (foreman decision, recorded here): log.doses = array (max 10) of {name: 1-60 chars, ml_per_l: 0-100}, optional, default []; nutrients text becomes optional (max 500 kept); new collection reservoir_events {id, plant_id, plant_name, date YYYY-MM-DD, kind: change|topoff, volume liters 0-100000, ec?, ph?, notes <=500} with routes GET /reservoir?plant_id=, POST /reservoir, PUT /reservoir/:id, DELETE /reservoir/:id, cascade on plant delete, included in backup (readData) and prepareImport (array forced, nextReservoirEventId rebuilt); schemaVersion stays 2 because every addition is filled by normalize(); CSV export gains a doses column 'Name 2 ml/L; Name 1 ml/L'; PUT /logs/:id accepts multipart with an optional image, sniffed like POST, old file unlinked. Height stays required on every log. Readers of the nutrients-required assumption outside this footprint: frontend LogViewer.jsx saveLog toast 'Nutrients are required' (owned by MR-41).
Accept: npm test green with the count RISING: POST /logs with doses [{name:'Part A', ml_per_l:2}] stores and returns them and GET /logs/export has a doses column reading 'Part A 2 ml/L'; a log with no nutrients text and no doses -> 201; doses with 11 entries or ml_per_l 101 -> 400; POST /reservoir {plant_id, date, kind:'change', volume: 20} -> 201, GET /reservoir?plant_id filters, DELETE /plants/:id removes its events, restore of a backup carrying reservoir_events keeps them and a later POST gets a non-colliding id; PUT /logs/:id multipart with a PNG replaces image_url and the old file is gone from uploads.
Red proof: Remove the doses validation loop -> the 400 test reads 201. Remove the cascade line -> the plant-delete test finds the event still listed. Remove the unlink in the PUT image path -> the old-file-gone assertion fails.

<!--row id=MR-38 tier=S status=done lane=F files=frontend/src/utils/vpd.js,frontend/src/utils/ranges.js,frontend/src/components/GrowthChart.jsx,frontend/src/components/PlantDetail.jsx,frontend/src/__tests__/vpd.test.js,frontend/src/__tests__/ranges.test.js commit=ec676d8-->
**MR-38 [S] VPD calculation, stage bands, chart series and out-of-range alert (TASKS 3)** Gap: grep -rni vpd frontend/src returns nothing. Build utils/vpd.js: vpdKpa(airTempC, humidityPct, leafOffsetC = -2) using the Tetens saturation-vapour-pressure formula (es(T) = 0.6108 * exp(17.27*T/(T+237.3)) kPa; VPD = es(leafT) - es(airT)*RH/100), rounded to 2 dp, null when either input is missing; VPD_BANDS keyed by GROWTH_STAGES: seedling and early_vegetative 0.4-0.8, vegetative and pre_flowering 0.8-1.2, every flowering stage and harvest_ready 1.2-1.6 kPa; vpdBand(stage) returns {min,max}. ranges.js measurementAlerts gains a vpd check (key 'vpd', label 'VPD', value computed from the log's air_temp and humidity, range vpdBand(stage)); describeAlert renders it with a ' kPa' suffix. PlantDetail chartData gains vpd per log; GrowthChart draws a VPD line (purple #A78BFA) on the right axis when any point has it, with its band shaded like pH/EC (pass vpdRange from PlantDetail). PlantDetail's alerts list already renders whatever measurementAlerts returns.
Accept: Unit: vpdKpa(25, 60) is 1.23 within 0.02; vpdKpa(25, null) is null; vpdBand('vegetative') is {min:0.8,max:1.2}. ranges.test: a vegetative log at 30C and 30% humidity produces an alert with key 'vpd' and status 'out'; a log at 24C/65% produces none. A PlantDetail render (existing test patterns) with two logs carrying air_temp+humidity shows the text 'VPD' in the chart legend area (assert the rendered string, 1.2). frontend lint 0 warnings; test:unit count rises.
Red proof: Change the Tetens constant 17.27 to 1.727 -> the 1.23 assertion fails. Delete the vpd entry from the checks array in measurementAlerts -> the 'out' test fails. Remove the VPD Line from GrowthChart -> the legend-text test fails.

<!--row id=MR-39 tier=S status=done lane=F files=frontend/src/utils/trends.js,frontend/src/data/assistantTips.js,frontend/src/__tests__/trends.test.js,frontend/src/__tests__/assistantTips.test.js commit=ad6c317-->
**MR-39 [S] EC and pH drift detection with Bud warning early (TASKS 5)** Gap: trends.js has phTrend only; grep -n ecTrend frontend/src returns nothing; assistantTips only reacts to phTrend and to point-in-time measurementAlerts, so a reading creeping toward the edge of the band over several days is never mentioned until it is already out. Add ecTrend(logs, ecRange) mirroring phTrend (3 monotonic readings, |delta| >= 0.2, leaving flag); add driftAlerts(logs, {phRange, ecRange}) returning entries {key, dir, value, min, max} for any of pH/EC where the last two readings are both outside the band OR the trend is leaving; assistantTips selectTip surfaces a drift tip at TIP_KINDS.alert priority with copy naming the measurement, direction and target band, deduped by a stable id so dismissal works. Read assistantTips.js in full first; keep its pure-function contract (now injected).
Accept: trends.test: ecTrend on EC 1.0,1.2,1.5 with range 0.8-1.4 returns {dir:'rising', leaving:true}; on 1.0,1.1,1.0 returns null; driftAlerts on two consecutive pH 6.9,7.0 with range 5.8-6.2 returns one entry with key 'ph'. assistantTips.test: given a plant snapshot whose logs carry that pH drift, selectTip returns a tip whose text contains 'pH' and 'drift' and whose kind is alert; with in-band readings no such tip appears.
Red proof: Change the 0.2 EC threshold to 2.0 -> the rising test returns null and fails. Delete the driftAlerts call from selectTip -> the 'drift' text test fails while the in-band negative still passes.

<!--row id=MR-40 tier=S status=done lane=F files=frontend/src/data/deficiencies.js,frontend/src/components/DeficiencyHelper.jsx,frontend/src/components/Assistant/AskMenu.jsx,frontend/src/components/Assistant/BudMascot.jsx,frontend/src/__tests__/deficiencies.test.js,frontend/src/__tests__/components/DeficiencyHelper.test.jsx commit=83dc93c-->
**MR-40 [S] Deficiency diagnosis helper: pick symptoms, get likely nutrient issue and fix (TASKS 7)** Gap: grep -rni 'deficien\|symptom' frontend/src hits only prose inside plantKnowledge.js stage 'problems' arrays ({issue, cause, solution}); nothing lets the user pick what they see and get a ranked answer. Build data/deficiencies.js: SYMPTOMS (about 14 entries: yellowing lower leaves, yellowing upper leaves, interveinal chlorosis, purple stems, brown leaf edges, curling leaves, brown spots, blossom end rot, stunted growth, wilting with wet roots, dark green clawed leaves, white powdery coating, slimy or brown roots, pale new growth), each with an id and label; ISSUES (nitrogen, phosphorus, potassium, calcium, magnesium, iron, overfeeding, root rot, pH lockout, powdery mildew) each listing symptom ids with weights and a fix string; diagnose(symptomIds) returns issues ranked by summed weight, ties broken by name, empty for no input. DeficiencyHelper.jsx: a Modal (components/ui/Modal.jsx) with symptom checkboxes and the top 3 results with fix text; opened from a new AskMenu item {key:'diagnose', label:'Something looks wrong'} handled in BudMascot (read how onPick routes keys; add the state to open the helper). Lint clean, keep the Bud tone light in copy.
Accept: deficiencies.test: diagnose(['yellowing_lower','stunted']) ranks nitrogen first; diagnose(['blossom_end_rot']) ranks calcium first; diagnose([]) is []. Component test: rendering DeficiencyHelper, ticking 'Purple stems' shows the text 'Phosphorus' in the results. frontend lint 0 warnings; count rises by at least 4.
Red proof: Zero the nitrogen weights -> the nitrogen-first test fails. Break the checkbox onChange (no state update) -> the 'Phosphorus' render test fails.

<!--row id=MR-42 tier=S status=done lane=F files=frontend/src/components/DashboardWidgets.jsx,frontend/src/components/PlantCards.jsx,frontend/src/__tests__/components/DashboardWidgets.test.jsx commit=ff1208e-->
**MR-42 [S] Dashboard widgets: next feeding, latest readings, alerts, harvest countdown (TASKS 10)** Gap: PlantCards.jsx Dashboard renders only the per-plant card grid; grep -n 'next feeding\|countdown' frontend/src/components returns nothing. Build DashboardWidgets.jsx: a 4-tile row above the grid using existing pure helpers only: Next feeding (earliest due schedule via utils/feeding feedingStatus, 'due today' / 'overdue N days' / 'in N days', plant name), Latest reading (most recent log across plants via utils/stats latestLog: plant, date via formatDate, pH/EC), Alerts (count of measurementAlerts across plants, red when > 0), Harvest (nearest harvestCountdown from utils/trends across plants, 'N days' or 'ready' or 'none in flowering'). Reads useAppData (plants, logs, schedules, settings, getPlantLogs) and useTheme colors like PlantCard. Rendered by Dashboard in PlantCards.jsx above the grid, and NOT in the no-plants empty state.
Accept: Component test mocking useAppData like Dashboard.test.jsx: two plants, one schedule fed 3 days ago on 'daily' -> the widget row shows the text 'overdue' and that plant's name; a plant with a pH 7.5 tomato log -> the Alerts tile shows '1'; a plant whose latest log is late_flowering -> the Harvest tile shows 'days'. Dashboard.test.jsx still passes. Lint 0 warnings.
Red proof: Make feedingStatus's due check always false inside the widget (mock or edit) -> the 'overdue' test fails. Hard-code the alerts count to 0 -> the '1' test fails.

<!--row id=MR-43 tier=S status=done lane=F files=frontend/src/utils/logFilter.js,frontend/src/components/LogViewer.jsx,frontend/src/__tests__/logFilter.test.js,frontend/src/__tests__/components/LogFilter.test.jsx commit=efa3d70-->
**MR-43 [S] Log search and filter: text, plant, date range, stage, measurement presence (TASKS 11)** Gap: grep -ni 'filter\|search' frontend/src/components/LogViewer.jsx returns nothing: the list is every log, always. Build utils/logFilter.js: filterLogs(logs, {q, plantId, from, to, stage, has}) pure: q matches plant_name, nutrients, notes case-insensitively; plantId exact; from/to inclusive on the date-only string (YYYY-MM-DD compares as text, see db/repository.js listLogs); stage exact; has is one of ph, ec, ppm, photo (image_url non-null). LogViewer gains a toolbar (search input with aria-label 'Search logs', plant select, two date inputs, stage select, has-measurement select, and a 'Clear filters' button) above the list, the header count reads 'Showing N of M logs' when a filter is active, and an empty result shows 'No logs match these filters'. Keep the existing edit/delete markup untouched.
Accept: logFilter.test: 8 cases covering each key plus combined; a q of 'tomato' matches plant_name case-insensitively; from='2026-06-02' excludes a 2026-06-01 log. Component test: render LogViewer with 3 mocked logs, type 'basil' into the search box -> only the basil log's plant name remains rendered and the header reads 'Showing 1 of 3 logs'. LogEdit.test.jsx and LogOrder.test.jsx still pass. Lint 0 warnings.
Red proof: Make filterLogs ignore q -> both the unit case and the component test fail. Remove the from comparison -> the from case fails.

<!--row id=MR-44 tier=S status=done lane=F files=frontend/src/hooks/useFeedingReminders.js,frontend/src/App.jsx,frontend/src/components/SettingsPanel.jsx,frontend/src/contexts/AssistantContext.jsx,frontend/src/__tests__/useFeedingReminders.test.jsx,main.js commit=1017c0e-->
**MR-44 [S] Feeding reminders as desktop notifications with a settings toggle (TASKS 16)** Gap: grep -rn Notification frontend/src main.js hits only the toast's aria-label; utils/feeding.js dueCount exists and nothing fires a system notification. Build hooks/useFeedingReminders.js: useFeedingReminders({schedules, enabled, now = () => new Date(), notify = defaultNotify, interval = 30 min}) which on mount and every interval computes due active schedules via feedingStatus and, for each not already notified today (localStorage key hydro.reminded = {scheduleId: 'YYYY-MM-DD'} using utils/dates todayLocalISO), calls notify(title, body) once; defaultNotify uses window.Notification when defined and permission is 'granted' or 'default' (Electron grants silently) and is a no-op otherwise. AssistantContext gains remindersEnabled (localStorage 'bud.remindersEnabled', default true) + setRemindersEnabled. SettingsPanel adds a Toggle 'Feeding reminders' with description 'Desktop notification when a feeding is due.' in the Fun & Effects card. App.jsx mounts the hook inside AppContent with schedules from useAppData and enabled from useAssistant. main.js: call app.setAppUserModelId('com.hydrotracker.hydro-growth-tracker') before whenReady so Windows shows notifications from the packaged app (the id must equal package.json build.appId; assert that in the test by reading package.json).
Accept: Hook test (renderHook from @testing-library/react): one schedule fed 2 days ago on 'daily' with an injected notify spy -> notify called once with a title containing the plant name; a second render on the same day -> not called again; enabled=false -> never called; a schedule fed today -> not called. A test reads main.js text and package.json and asserts the setAppUserModelId argument equals build.appId. SettingsPanel.test.jsx still passes and a new case flips the toggle and sees aria-checked change. Lint 0 warnings.
Red proof: Remove the already-notified check -> the 'not called again' test fails. Invert enabled -> the enabled=false test fails. Change the appId string in main.js -> the parity test fails.

<!--row id=MR-56 tier=H status=done lane=E files=TASKS.md commit=fef60dd-->
**MR-56 [H] TASKS.md: mark auto-backup (18) and installer smoke test (20) done, point every open item at its board row** Gap: TASKS.md item 18 (auto-backup on schedule) shipped in MR-3 as repo.dailyBackup (commit fcce417) and item 20 (installer smoke test) ran in mr-run1 (electron-builder --dir exit 0, exe smoke-launched); both still sit under To Do. Move both to Done with the sha/row, and append '(board MR-nn)' to each remaining To Do item using this mapping: 1->MR-37/MR-41, 2->MR-37/MR-46, 3->MR-38, 4->MR-53, 5->MR-39, 6->MR-48, 7->MR-40, 8->MR-49, 9->MR-47, 10->MR-42, 11->MR-43, 12->MR-50, 13->MR-51, 14->MR-52, 15->MR-45, 16->MR-44, 17->MR-54, 19->MR-55. Update the _Last updated_ line to 2026-09-03. Change nothing else.
Accept: grep -c 'board MR-' TASKS.md prints 18; items 18 and 20 appear under Done and not under To Do; python tools/board.py lint stays green.
Red proof: Leave item 3 unmapped -> the grep count reads 17, not 18 (the count is the check).

<!--row id=MR-41 tier=S status=done lane=F deps=MR-37,MR-43 files=frontend/src/components/AddLogForm.jsx,frontend/src/components/LogViewer.jsx,frontend/src/__tests__/components/AddLogForm.test.jsx,frontend/src/__tests__/components/LogEdit.test.jsx commit=cfa123a-->
**MR-41 [S] Dosing UI: dose rows on the add and edit log forms, shown in the log list and CSV (TASKS 1 frontend half)** Gap: After MR-37 the server stores log.doses and no longer requires nutrients text; grep -n doses frontend/src/components returns nothing and LogViewer.saveLog still toasts 'Nutrients are required' (a stale reader of the old rule, CODING-PRACTICES 1.4b-2). AddLogForm: below 'Nutrients used' add a 'Doses' editor: rows of nutrient name + ml/L number with add/remove buttons (max 10), included in the payload as doses (JSON string when sent as FormData with an image; the server parses a string), validated through validateLogByField which MR-37 extends. LogViewer: render doses as pills 'Part A 2 ml/L' under the nutrients line, edit form gets the same editor, remove the nutrients-required toast. Draft persistence (localStorage logFormDraft) must include doses.
Accept: AddLogForm.test: adding two dose rows and submitting posts a payload whose doses array has both entries with numeric ml_per_l; submitting with empty nutrients text and no doses no longer shows a nutrients error. LogEdit.test: editing a log with doses shows the rows prefilled and saving sends them; a log rendered with doses shows the text '2 ml/L'. Lint 0 warnings.
Red proof: Drop doses from the submit payload -> the two-entry assertion fails. Restore the nutrients-required toast -> the empty-nutrients save test fails.

<!--row id=MR-46 tier=S status=done lane=F deps=MR-37,MR-38 files=frontend/src/contexts/AppDataContext.jsx,frontend/src/components/ReservoirLog.jsx,frontend/src/components/PlantDetail.jsx,frontend/src/__tests__/components/AppDataContext.test.jsx,frontend/src/__tests__/components/ReservoirLog.test.jsx commit=7ba301d-->
**MR-46 [S] Reservoir change tracker UI: log changes and top-offs, see when water was last refreshed (TASKS 2 frontend half)** Gap: MR-37 adds GET/POST/PUT/DELETE /reservoir; grep -n reservoir frontend/src/contexts returns nothing, so the renderer never loads or writes events. AppDataContext loads reservoirEvents in loadAll (5th parallel GET) and exposes createReservoirEvent, updateReservoirEvent, deleteReservoirEvent, getPlantReservoirEvents(plant). ReservoirLog.jsx: a card in PlantDetail (below the schedules card) with a 'Last full change: N days ago (date)' line (or 'never logged'), a list of events newest first (kind, volume in the display volume unit via utils/format, date via formatDate, notes, delete with ConfirmDialog), and an inline add form (kind select change/top-off, volume in display units converted with toLiters, date defaulting to todayLocalISO, optional EC/pH/notes). Toasts on success/failure like other forms.
Accept: AppDataContext.test: loadAll issues a GET /reservoir and exposes the events; createReservoirEvent posts to /reservoir and refreshes. ReservoirLog.test: with one change event 3 days before the injected now, the text 'Last full change: 3 days ago' renders; submitting the add form with volume 5 gallons under gallon units calls createReservoirEvent with volume 18.93 (2 dp); an empty list renders 'never logged'. Lint 0 warnings.
Red proof: Remove the /reservoir GET from loadAll -> the exposure test fails. Skip toLiters in the add path -> the 18.93 assertion reads 5.

<!--row id=MR-47 tier=S status=done lane=F deps=MR-37,MR-42 files=frontend/src/components/QuickLogForm.jsx,frontend/src/components/PlantCards.jsx,frontend/src/__tests__/components/QuickLogForm.test.jsx commit=5e81dc4-->
**MR-47 [S] Quick-log fast entry: pH, EC and height in one strip on the dashboard (TASKS 9)** Gap: grep -rni 'quick.?log' frontend/src returns nothing; the only way to log is the full Add Log form. After MR-37 a log needs only plant, date and height. Build QuickLogForm.jsx: one row with plant select (defaults to the first plant), height (display units, converted with toCm), pH, EC, and a 'Quick log' button; date is todayLocalISO; validates through validateLogByField from @shared/validation; calls createLog and toasts 'Logged' then clears pH/EC but keeps the plant. Rendered by Dashboard in PlantCards.jsx between the widgets row (MR-42) and the card grid, hidden in the empty state.
Accept: Component test: with two plants and settings length 'in', entering height 10, pH 6.0, EC 1.2 and submitting calls createLog with height 25.4, ph 6, ec 1.2, plant_id of the chosen plant and date equal to todayLocalISO(); submitting with height blank shows the shared validator's height message and does not call createLog. Dashboard.test.jsx still passes. Lint 0 warnings.
Red proof: Skip toCm -> the 25.4 assertion reads 10. Delete the validate call -> the blank-height test calls createLog.

<!--row id=MR-48 tier=S status=done lane=F deps=MR-38 files=frontend/src/utils/idealCurve.js,frontend/src/components/GrowthChart.jsx,frontend/src/components/PlantDetail.jsx,frontend/src/__tests__/idealCurve.test.js commit=c9ace1a-->
**MR-48 [S] Grow-vs-ideal overlay: expected height curve from the species profile behind the real line (TASKS 6)** Gap: grep -rni 'ideal.?curve\|expected height' frontend/src returns nothing. plantKnowledge PLANT_PROFILES[species].stages[stage] carry heightRange {min,max} cm and duration strings like '14-21 days' (trends.js parseMaxDays shows the parsing). Build utils/idealCurve.js: idealHeightAt(species, daysSinceStart) walks the stages in GROWTH_STAGES order accumulating the midpoint duration of each and linearly interpolating heightRange midpoints, returning cm (null when the species has no stages or days < 0); idealSeries(species, startDate, dates) maps each log date to the ideal cm via utils/dates. PlantDetail: when plant.start_date is set, chartData gains ideal per point (converted to display units like height); GrowthChart draws it as a dashed grey line named 'Ideal' on the left axis when present, and a small caption 'Ideal curve from the <species> profile' under the chart.
Accept: idealCurve.test: for tomato, idealHeightAt at day 0 is the seedling min-mid (>= 0 and < 15); at day 40 it is greater than at day 20; a species with no profile ('generic' has stages, so use an unknown string) returns null; days -1 returns null. A PlantDetail render with start_date and two logs shows the legend text 'Ideal'; without start_date it does not. Lint 0 warnings.
Red proof: Return a constant from idealHeightAt -> the day-40 > day-20 test fails. Remove the ideal Line -> the legend test fails while the no-start_date negative still passes.

<!--row id=MR-49 tier=S status=done lane=F deps=MR-39 files=frontend/src/utils/trends.js,frontend/src/data/assistantTips.js,frontend/src/__tests__/trends.test.js,frontend/src/__tests__/assistantTips.test.js commit=6d7233b-->
**MR-49 [S] Harvest prediction with a confidence range from observed stage transitions and growth rate (TASKS 8)** Gap: trends.harvestCountdown anchors on the first late_flowering log plus the profile's max late-flower duration: one number, no range, and it ignores how the plant has actually been pacing. Replace with harvestCountdown(logs, species, now) returning {ready, days, low, high, confidence: 'high'|'medium'|'low'} where low/high come from the profile's min and max late-flower durations, the pacing factor is (observed duration of the previous completed stage) / (profile midpoint duration of that stage) clamped 0.5-2 when the logs carry that transition (else 1), confidence is high when 2+ completed stage transitions are observed, medium with 1, low with none; keep the existing ready semantics. assistantTips harvest copy shows 'about N days (M-K)'. Keep every existing trends test green and extend it.
Accept: trends.test: a plant whose logs show vegetative starting day 0, flowering day 30 and late_flowering day 60 for tomato yields low <= days <= high and confidence 'high'; only a late_flowering log yields confidence 'low' with low < high; a harvest_ready log yields ready true. assistantTips.test: the harvest tip text contains the range in the form 'N-M'.
Red proof: Set low = high = days -> the low < high assertion fails. Return confidence 'high' unconditionally -> the 'low' case fails.

<!--row id=MR-54 tier=O status=done lane=F deps=MR-37 files=server.js,test/**,package.json,package-lock.json,frontend/src/components/BackupRestore.jsx,frontend/src/__tests__/components/BackupRestore.test.jsx commit=5e2b32a-->
**MR-54 [O] Backup with photos: zip export and restore that carries the uploads folder (TASKS 17)** Gap: GET /backup returns JSON only and BackupRestore.jsx says 'Photos aren't included'; grep -n zip server.js returns nothing. Add adm-zip (root dependency) and GET /backup/zip: a zip holding backup.json (the same envelope as GET /backup) and uploads/<file> for every image referenced by a log; POST /backup/restore/zip: multipart zip (50 MB cap), entries validated (backup.json must be our envelope, upload entries must be flat names with image magic bytes per sniffImageExt, path traversal refused), then the same migrate/prepareImport/snapshot path as JSON restore, uploads written after the store save. BackupRestore.jsx: 'Download backup with photos' and 'Restore from zip...' buttons beside the JSON ones, copy updated to say the zip includes photos.
Accept: npm test: a store with one photo log -> the zip contains backup.json and uploads/<name> whose bytes equal the file on disk; restoring that zip into a fresh server yields the log with the same image_url and the file present; a zip entry named ../evil.png -> 400 and no file written outside uploads; a zip whose backup.json is not our envelope -> 400 and store unchanged. Component test: the two new buttons render and the download calls /backup/zip.
Red proof: Remove the traversal check -> the ../evil.png test writes the file and fails. Remove the envelope check -> the not-our-envelope test reads 200.

<!--row id=MR-50 tier=S status=done lane=F deps=MR-47 files=frontend/src/components/CompareChart.jsx,frontend/src/components/PlantCards.jsx,frontend/src/__tests__/compareSeries.test.js,frontend/src/__tests__/components/CompareChart.test.jsx commit=81fe9c0-->
**MR-50 [S] Multi-plant compare overlay: several plants on one chart by day since start (TASKS 12)** Gap: grep -rni compare frontend/src/components returns only Modal.jsx prose; every chart is single-plant. Build CompareChart.jsx exporting a pure compareSeries(plants, getPlantLogs, metric) that aligns each plant's logs by days since its first log (x) and the chosen metric (height in display units, ph, ec) into recharts rows [{day, <plantName>: value, ...}], and a component with plant checkboxes (max 6), a metric select, and a recharts LineChart with one line per selected plant. Rendered by Dashboard in PlantCards.jsx as a collapsible 'Compare plants' card below the grid when there are 2+ plants.
Accept: compareSeries.test: two plants with logs on days 0 and 7 produce rows for day 0 and day 7 carrying both names; a plant with no logs is skipped. Component test: ticking two plants renders both plant names in the legend text; the card does not render with one plant. Lint 0 warnings.
Red proof: Align by absolute date instead of day-since-first -> the day-0 row test fails (the plants started in different months in the fixture, assert that precondition).

<!--row id=MR-51 tier=S status=done lane=F deps=MR-41,MR-46 files=frontend/src/components/PhotoTimeline.jsx,frontend/src/components/PlantDetail.jsx,frontend/src/components/LogViewer.jsx,frontend/src/__tests__/components/PhotoTimeline.test.jsx,frontend/src/__tests__/components/LogEdit.test.jsx commit=0a69c85-->
**MR-51 [S] Photo timeline in the plant view, and replacing a photo when editing a log (TASKS 13 frontend half)** Gap: grep -rni 'timeline\|slider' frontend/src returns nothing; photos only appear as a thumbnail per log entry. MR-37 makes PUT /logs/:id accept an image. Build PhotoTimeline.jsx: given the plant's logs with image_url sorted by date, a large image, an input type=range (aria-label 'Photo timeline') that scrubs through them, prev/next buttons, and a caption with formatDate(log.date) plus height; rendered in PlantDetail above the history table only when 2+ photos exist. LogViewer edit form: a file input 'Replace photo' that, when set, sends the update as multipart FormData (the same shape AddLogForm uses) so the server replaces the image.
Accept: PhotoTimeline.test: three photo logs -> the range max is 2 and moving it to 1 shows the middle log's date caption; one photo -> the component returns null. LogEdit.test: choosing a file in the edit form makes updateLog receive a FormData whose 'image' entry is the file.
Red proof: Ignore the range value -> the caption test fails. Send JSON instead of FormData when a file is chosen -> the FormData assertion fails.

<!--row id=MR-45 tier=S status=done lane=F deps=MR-40,MR-49 files=frontend/src/utils/achievements.js,frontend/src/components/Achievements.jsx,frontend/src/components/Assistant/AskMenu.jsx,frontend/src/components/Assistant/BudMascot.jsx,frontend/src/data/assistantTips.js,frontend/src/__tests__/achievements.test.js,frontend/src/__tests__/components/Achievements.test.jsx,frontend/src/__tests__/assistantTips.test.js commit=6a8c232-->
**MR-45 [S] Achievements and badges that Bud celebrates (TASKS 15)** Gap: grep -rni 'achievement\|badge' frontend/src hits only comments. Build utils/achievements.js: BADGES (first_plant, first_log, ten_logs, hundred_logs, streak_7, streak_30 via trends.careStreak, first_harvest via a harvest_ready log, photo_fan for 10 photos, chemist for 20 logs with pH and EC) each {id, label, description, emoji}; earnedBadges({plants, logs}, now) returns the earned ids in BADGES order; newlyEarned(prevIds, currentIds). Achievements.jsx: a Modal grid of all badges, earned ones lit, opened from a new AskMenu item {key:'badges', label:'Show my badges'} routed in BudMascot. assistantTips: a milestone tip when a badge is newly earned (BudMascot keeps the last-seen ids in localStorage 'bud.badges' and passes newlyEarned into the snapshot). Coordinate with MR-40's AskMenu item order: append after it.
Accept: achievements.test: 10 logs -> ten_logs earned and hundred_logs not; a harvest_ready log -> first_harvest; newlyEarned(['first_log'], ['first_log','ten_logs']) is ['ten_logs']. Component test: earned badges render with aria-label 'earned'. assistantTips.test: a snapshot with newBadges ['ten_logs'] yields a milestone tip containing the badge label.
Red proof: Change the ten_logs threshold to 11 -> the 10-log test fails. Drop the newBadges branch from selectTip -> the milestone test fails.

<!--row id=MR-52 tier=S status=done lane=F deps=MR-45,MR-46 files=frontend/src/data/assistantTips.js,frontend/src/components/Assistant/BudMascot.jsx,frontend/src/__tests__/assistantTips.test.js commit=c9be6b0-->
**MR-52 [S] Bud health-reactive expansion: reacts to reservoir age, drift, VPD and stage changes, and knows more tips (TASKS 14)** Gap: After MR-39/45/46 the snapshot BudMascot builds still omits reservoir events and VPD. Extend the snapshot with reservoirEvents (from getPlantReservoirEvents) and the latest log's VPD (utils/vpd), and add tips: reservoir not changed for 14+ days (reminder), VPD out of band (alert, via measurementAlerts already), a stage-change congratulation when the inferred stage differs from the previous log's (milestone), and 8 new idle tips. Keep every tip id stable so dismissals persist.
Accept: assistantTips.test: a snapshot whose newest reservoir change is 15 days old yields a reminder tip containing 'reservoir'; 13 days yields none; a snapshot whose last two logs infer different stages yields a milestone containing the new stage label; IDLE_TIPS length is at least 14.
Red proof: Change 14 to 140 -> the 15-day test fails. Remove the stage comparison -> the stage milestone test fails.

<!--row id=MR-53 tier=O status=done lane=F deps=MR-41,MR-46,MR-44 files=db/repository.js,server.js,test/**,frontend/src/utils/cost.js,frontend/src/components/SettingsPanel.jsx,frontend/src/components/CostCard.jsx,frontend/src/__tests__/cost.test.js,frontend/src/__tests__/components/CostCard.test.jsx,frontend/src/__tests__/components/SettingsPanel.test.jsx commit=92b2c70-->
**MR-53 [O] Water and cost tracking: consumption per grow and running cost from nutrient prices (TASKS 4)** Gap: grep -rni 'cost\|price' frontend/src returns nothing. Settings gain nutrient_prices: array of {name, price_per_liter} (repository.updateSettings validates: name 1-60 chars, price 0-100000, max 20 entries; server test). utils/cost.js: waterUsed(events) = sum of volume over change and topoff events (liters); nutrientUsed(logs, events) = per dose name, sum over logs of ml_per_l x the reservoir volume in force at that log (the latest change event on or before the log date, else the plant's reservoir_volume, else 0) in ml; grandCost(usage, prices) with unknown names costed 0 and listed as 'unpriced'. CostCard.jsx in PlantDetail: water used, nutrient ml per product, total cost in a plain currency-less number, and a note listing unpriced products. SettingsPanel: a 'Nutrient prices' editor (name + price per liter) saved with the rest.
Accept: cost.test: one change of 20 L and a top-off of 5 L -> waterUsed 25; a log with dose Part A 2 ml/L on a 20 L reservoir -> 40 ml; prices Part A 30 per liter -> cost 1.2; an unpriced name is reported. Server test: PUT /settings with nutrient_prices saves and clamps; a 21-entry list -> 400. CostCard render shows 'Part A' and '1.20'. SettingsPanel test saves prices.
Red proof: Ignore top-offs -> waterUsed reads 20. Use the plant's reservoir_volume even when a change event exists -> the 40 ml test fails (fixture sets them to differ, assert that).

<!--row id=MR-55 tier=O status=done lane=F deps=MR-54 files=server.js,test/**,frontend/src/components/BackupRestore.jsx,frontend/src/__tests__/components/BackupRestore.test.jsx commit=af05907-->
**MR-55 [O] CSV import of logs from another tracker with a dry-run preview (TASKS 19)** Gap: grep -n 'logs/import' server.js returns nothing. POST /logs/import {csv, dryRun}: parse CSV (header row; recognise our own export columns plus aliases: plant/plant_name, date, height, height_unit, ph, ec, ppm, nutrients, notes, growth_stage; ignore unknown columns), run every row through validateLog, resolve plants by name (create when missing, activeOnly rules from MR-4), and return {rows, valid, errors:[{line, messages}]}; when dryRun is false and errors is empty, create the logs in one write and return counts. Formula-prefixed cells from our own export (a leading quote) are stripped back. BackupRestore.jsx: 'Import logs from CSV...' picks a file, shows the dry-run summary in a Modal (N rows, M valid, first 5 errors), and commits on confirm.
Accept: Server test: importing our own export of 3 logs round-trips heights, pH and dates exactly; a row with pH 15 is reported at its line number and nothing is written in dry run; the non-dry import of a clean file creates the plants it names; a 5 MB body is refused. Component test: the dry-run summary text 'valid' renders and confirm posts with dryRun false.
Red proof: Skip validateLog per row -> the pH 15 test reports 0 errors. Write in dry-run -> the nothing-written assertion fails.

<!--row id=MR-58 tier=S status=done lane=D deps=MR-41,MR-46,MR-47,MR-44,MR-54 flags=tree files=e2e/** commit=95553f4-->
**MR-58 [S] Playwright e2e coverage for the new features: quick log, reservoir event, dosing, zip backup** Gap: e2e/app.spec.mjs has 10 specs, none touching the wave-2 features. Add e2e/features.spec.mjs using helpers.mjs: quick-log a pH on the dashboard and see it in the log list; add a reservoir change on the plant view and see 'Last full change'; add a log with a dose row and see '2 ml/L' in View Logs; download the zip backup and assert the file on disk is a zip (PK magic) that contains backup.json. Solo run (flags=tree): npm run test:e2e.
Accept: npm run test:e2e passes with 4 more specs than before (16 -> 20) on Electron; each spec asserts rendered text, never state.
Red proof: Change the expected dose text to '3 ml/L' -> that spec fails, proving it reads the screen.

<!--row id=MR-57 tier=S status=done lane=E deps=MR-50,MR-51,MR-52,MR-53,MR-55,MR-58,MR-56 files=docs/user-guide.md,CHANGELOG.md,package.json,frontend/package.json,tools/changelog_from_board.py,README.md commit=0e8eb4f-->
**MR-57 [S] Docs and version for the feature release: user guide sections, changelog lane F, version 1.3.0** Gap: docs/user-guide.md has no section for doses, reservoir tracking, VPD, quick log, filters, reminders, compare, photo timeline, badges, cost, zip backup or CSV import; tools/changelog_from_board.py LANES lacks F so the generator drops every feature row. Add LANES F = 'Features', bump both package.json versions to 1.3.0, generate the 1.3.0 section with the tool (python tools/changelog_from_board.py 1.3.0 <date>) and prepend it, write one short user-guide subsection per feature (what it does, where it is), update README's feature list.
Accept: python tools/changelog_from_board.py 1.3.0 2026-09-03 lists every done lane-F row; both package.json read 1.3.0; grep -c '^### ' docs/user-guide.md rises by at least 10; npm test and board lint stay green.
Red proof: Remove F from LANES -> the generator output has zero feature lines (assert the count before and after).

<!--row id=MR-59 tier=H status=done lane=F deps=MR-53,MR-48,MR-51 files=frontend/src/components/PlantDetail.jsx,frontend/src/__tests__/components/CostCard.test.jsx commit=7ed2c2b-->
**MR-59 [H] Mount CostCard in the plant view** Gap: MR-53 builds CostCard and its tests against props but PlantDetail.jsx is claimed by MR-48 and MR-51 in the same window; the one-line mount is split out so the three rows serialise on the board instead of in the foreman's head.
Accept: PlantDetail renders CostCard below the reservoir card; a render test in CostCard.test.jsx mounts PlantDetail with one log carrying a dose and one change event and finds the text 'Water used'.
Red proof: Remove the CostCard element from PlantDetail -> the PlantDetail render test fails.

<!--row id=MR-60 tier=S status=done lane=D files=frontend/src/test-utils.jsx,frontend/src/__tests__/**,frontend/src/components/QuickLogForm.jsx commit=3e3924a-->
**MR-60 [S] One renderWithProviders helper for component tests; retire the per-component toast fallback** Gap (retrospective, mr-run2, two incidents): MR-46 mounted ReservoirLog (which toasts) inside PlantDetail and two older tests that render PlantDetail bare went red; MR-47 worked around the same class by giving QuickLogForm a try/catch useOptionalToast because Dashboard.test.jsx renders without a ToastProvider. grep -rln ToastProvider frontend/src/__tests__ = 3 files hand-wrap it today. Add frontend/src/test-utils.jsx exporting renderWithProviders(ui) = ThemeProvider + ToastProvider + the matchMedia and ResizeObserver stubs; migrate every component test to it; delete useOptionalToast from QuickLogForm and use useToast directly.
Accept: grep -rn 'useOptionalToast' frontend/src returns nothing; every file under frontend/src/__tests__/components imports renderWithProviders and none imports ThemeProvider directly; npm --prefix frontend run test:unit stays green with the same count.
Red proof: Remove ToastProvider from renderWithProviders -> the QuickLogForm and ReservoirLog tests fail with the provider error.

<!--row id=MR-61 tier=S status=done lane=D files=tools/validate_worktree.sh,tools/test_validate_worktree.sh,board.config.json commit=3d96855-->
**MR-61 [S] Validate on a detached worktree so in-flight worker edits never poison the ladder** Gap (retrospective, mr-run2): the MR-36 exclude list only removes in-flight TEST files, but in-flight SOURCE edits (a half-written LogViewer.jsx) still red the suite; the foreman ran the whole ladder on a detached git worktree with node_modules junctioned in (a scratch script, 6 uses this run, every full-ladder verdict came from it). Promote it: tools/validate_worktree.sh <sha> creates or reuses ../hydro-wt at that sha, junctions/symlinks both node_modules, runs build, backend, lint, unit, board lint, e2e with each RC captured (never piped), prints the summary, exits non-zero on any red; board.config.json work.verify names it.
Accept: tools/validate_worktree.sh HEAD prints one line per gate with its RC and the test counts, exits 0 on a green tree; a smoke test script proves it exits non-zero when a gate is red (stage a failing test in the worktree copy only).
Red proof: Point the script at a sha with a deliberately failing test -> exit code is non-zero and the summary names the gate.

<!--row id=MR-62 tier=S status=done lane=F files=frontend/src/utils/budBus.js,frontend/src/hooks/useBudEvent.js,frontend/src/components/Assistant/BudMascot.jsx,frontend/src/components/Assistant/BudRenderer.jsx,frontend/src/components/Assistant/BudLeaf.jsx,frontend/src/__tests__/budBus.test.js,frontend/src/__tests__/components/BudMascot.test.jsx commit=7feb7e8-->
**MR-62 [S] Bud event bus and cue plumbing: the app can tell Bud what the user is doing, and Bud's body receives named cues** Gap: grep -rn 'budBus\|cue' frontend/src returns only data/budCues.js (the shared contract, committed by the foreman): nothing emits app events and BudThree receives only expression/mood/shades/talking props. Build utils/budBus.js (emit(type, payload), subscribe(type|'*', fn) -> unsubscribe; validates type against BUD_EVENT_NAMES and throws on an unknown one) and hooks/useBudEvent.js (subscribe for a component's lifetime). BudMascot subscribes and maps events to cues: form:reading status ok -> nod, warn/out -> wince, form:focus -> peek, save:ok -> cheer, save:error -> facepalm, delete -> sulk, app:return with awaySeconds >= 600 -> welcomeBack, tab settings -> yawn; it holds a `cue` state {name, at} and passes it through BudRenderer to BudThree (BudThree is MR-64's file: this row only passes the prop; the SVG BudLeaf fallback maps nod/cheer/wince/sulk to a class name so reduced-motion users get a static hint). BudMascot also emits app:return itself from a visibilitychange listener (last hidden timestamp). Cues do NOT open a speech bubble and do not touch the 60 s tip rate limit.
Accept: budBus.test: subscribe/emit round trip, unsubscribe stops delivery, unknown type throws, '*' receives everything. BudMascot.test (new; mock useAppData/useAssistant like other component tests and mock BudRenderer to a stub that records its props): emitting form:reading {status:'out'} makes the stub receive cue.name 'wince'; save:ok -> 'cheer'; delete -> 'sulk'; two identical events 100 ms apart yield two distinct cue.at stamps.
Red proof: Delete the mapping entry for 'out' -> wince: the wince test fails while cheer still passes. Remove the throw for unknown types -> the unknown-type test fails.

<!--row id=MR-64 tier=S status=done lane=F files=frontend/src/components/Assistant/BudThree.jsx,frontend/src/components/Assistant/lush/buildLush.js commit=55d9440-->
**MR-64 [S] Bud's body performs the reaction cues: peek, nod, wince, cheer, sulk, shrug, facepalm, point, land** Gap: BudThree.jsx has poses for idle/happy/alert/celebrating, five idle emotes and a cough, but no way to perform a one-shot cue on request; grep -n cue BudThree.jsx returns nothing. Add a `cue` prop ({name, at}); a cueRef edge-detects a new `at` and starts a cue machine (phase, t) alongside the idle emote machine; cueFor() from data/budCues validates the name and the record's `interrupts` decides whether it cuts an idle emote short (cooldown) or waits. Implement body language for: peek (lean toward the cursor side, lids narrow, brows down, small forward tilt), nod (two head/body dips, small smile), wince (recoil back 8 percent, brows knit, eyes squint, mouth small), cheer (both arms up via new ARM_CHEER poses in buildLush, bounce, big smile, party-hat-free), sulk (droop: body tilts down, lids half, mouth frown via the existing lips flip, arms hang), shrug (arms out sideways ARM_SHRUG, brows up, mouth flat, a small head tilt), facepalm (right arm to the face ARM_FACEPALM, eyes shut, body dips), point (right arm straight out toward +X or -X per payload.dir, gaze same way), land (a squash-and-settle like the drag drop wobble). Every cue eases in and out through the existing lerp style, respects `document.hidden`, and never leaves a pose behind: when the cue ends the loop returns to the expression pose. Extend the DEV window.__bud handle with cue(name) so the foreman can capture frames.
Accept: Manual capture by the foreman (jsdom has no WebGL): with the dev server up, window.__bud.cue('wince') etc. produce visibly different frames for all nine cues, each returning to rest within its dur + 1 s; frontend lint 0 warnings; the existing SVG fallback and unit suite stay green (BudThree is untested by design, see its header comment).
Red proof: Set the cue machine to ignore `at` (never start) -> window.__bud.cue('cheer') produces no change in the captured frame against the rest frame (the foreman's capture strip is the instrument; pixel diff of cheer vs rest must be non-zero with the machine live and zero with it disabled).

<!--row id=MR-67 tier=S status=done lane=F files=frontend/src/data/assistantTips.js,frontend/src/data/budVoice.js,frontend/src/__tests__/assistantTips.test.js,frontend/src/__tests__/budVoice.test.js,frontend/src/contexts/AssistantContext.jsx,frontend/src/components/SettingsPanel.jsx,frontend/src/__tests__/components/SettingsPanel.test.jsx commit=7e98c80-->
**MR-67 [S] Towelie voice: forgetful, spacey, over-helpful copy for every tip, with a clean-voice switch in Settings** Gap: assistantTips.js copy is 'lightly cannabis-punny, family-friendly'; the owner ruled 2026-09-03 for a full Towelie personality (South Park's talking towel: spacey, forgets what he was saying mid-sentence, over-helpful reminders, 'you wanna get high?', 'I have no idea what's going on', then snaps back to a genuinely useful point) with an in-app switch to a clean voice. Build data/budVoice.js: VOICES = { towelie, clean }, each a table of the SAME keys (greeting, idle pool, alert prefix, reminder prefix, milestone, harvest, drift, reservoir, badge, tangent pool, catchphrases) so every tip is composed from voice(key) + the factual clause; the factual clause (numbers, ranges, plant names) is never inside the voice table so it cannot be lost in a joke. Towelie lines: at least 12 tangents ('Wait, what were we... oh right, pH.'), 8 catchphrases, and the reminder shape 'Don't forget to bring a ... I mean, log your pH.' Family-friendly is NOT required for the towelie voice; keep it PG-13 (no slurs, nothing sexual). assistantTips' buildCandidates/answerQuestion take `voice` from the input snapshot (default 'towelie'); AssistantContext gains voice (localStorage 'bud.voice', default 'towelie') + setVoice; SettingsPanel adds a 'Bud's voice' select with 'Towelie (spacey)' and 'Clean' under Fun & Effects.
Accept: budVoice.test: both voices define every key; the towelie tangent pool has >= 12 entries; no voice string contains a plant name or a number placeholder (the facts come from the clause). assistantTips.test: the same pH alert snapshot rendered with voice 'towelie' contains the factual band '5.8-6.2' AND a towelie marker (any catchphrase), and with voice 'clean' contains the band and NO towelie marker; existing tests updated to pass a voice. SettingsPanel.test: changing the select calls setVoice('clean').
Red proof: Make the towelie composer drop the factual clause -> the '5.8-6.2' assertion fails for towelie while clean passes. Return the towelie table for both voices -> the 'clean has no marker' test fails.

<!--row id=MR-69 tier=S status=done lane=F files=frontend/src/utils/sound.js,frontend/src/__tests__/sound.test.js commit=27fcace-->
**MR-69 [S] Bud sounds: huh, hum, chomp, yawn, steps, giggle - tiny synthesized cues in utils/sound.js** Gap: utils/sound.js exports pop, flick, snore only; data/budCues.js names sounds huh, hum, chomp, yawn, steps that do not exist yet (grep -n 'export function' frontend/src/utils/sound.js). Add them in the same WebAudio-oscillator style, each tiny and gated by the enabled flag: huh (two-note falling 'uh?'), hum (a 2.5 s soft triangle wave with slow vibrato), chomp (three short noise-ish bursts), yawn (a slow rising then falling sine ~1.4 s), steps (one soft low tick; the walker calls it per step), giggle (three quick rising blips). Export a SOUNDS map { name: fn } so the rig can call sounds by the string in the cue table, and playCue(name) that looks it up and is a no-op for null/unknown.
Accept: sound.test (existing file; read how it stubs AudioContext): every name in CUES' sound slots resolves in SOUNDS; playCue('nope') does not throw; each new function schedules at least one oscillator on the stub when enabled and none when disabled.
Red proof: Remove 'hum' from SOUNDS -> the cue-slot resolution test fails naming hum.

<!--row id=MR-63 tier=S status=done lane=F deps=MR-62 files=frontend/src/components/AddLogForm.jsx,frontend/src/components/QuickLogForm.jsx,frontend/src/components/LogViewer.jsx,frontend/src/components/PlantManager.jsx,frontend/src/components/ReservoirLog.jsx,frontend/src/components/FeedingSchedule.jsx,frontend/src/App.jsx,frontend/src/__tests__/budEmitters.test.jsx commit=cc01722-->
**MR-63 [S] The app tells Bud what you are doing: readings as you type, saves, deletes, errors, tab changes** Gap: After MR-62 the bus exists but grep -rn 'budBus' frontend/src/components returns nothing. Emit: AddLogForm and QuickLogForm on focus of the first field -> form:focus; on change of ph/ec/humidity/air_temp -> form:reading {field, value, status} where status comes from utils/ranges classify() against the selected plant's profile/stage band (unknown when no plant); on successful submit -> save:ok {kind:'log'}; on validation failure -> save:error. LogViewer: delete confirmed -> delete {kind:'log'}; save -> save:ok. PlantManager: add -> save:ok {kind:'plant'}, delete -> delete {kind:'plant'}. ReservoirLog add -> save:ok {kind:'reservoir'}, delete -> delete. FeedingSchedule create/mark fed -> save:ok {kind:'schedule'}. App.jsx: tab change -> tab {tab}. Throttle form:reading to one emit per 400 ms per field so typing does not spam.
Accept: budEmitters.test (subscribe to the real bus, render each form with mocked contexts via renderWithProviders): typing 7.5 into AddLogForm's pH with a tomato plant selected emits form:reading {field:'ph', status:'out'}; typing 6.0 emits status 'ok'; a successful QuickLog submit emits save:ok {kind:'log'}; confirming a delete in LogViewer emits delete {kind:'log'}; switching tab in App emits tab. All existing component tests stay green.
Red proof: Remove the AddLogForm pH emit -> the 'out' test fails; remove the LogViewer delete emit -> the delete test fails.

<!--row id=MR-68 tier=S status=done lane=F deps=MR-64,MR-69 files=frontend/src/components/Assistant/BudThree.jsx,frontend/src/components/Assistant/lush/buildLush.js commit=a7aead9-->
**MR-68 [S] More idle life: looks around, scratches, hums, munchies at noon, yawns late, checks his watch, welcomes you back** Gap: The idle roulette in BudThree.jsx has five bits with fixed weights and no time-of-day bias beyond lids/breathing. Add the lookAround, scratch, hum, watch idle bits to the roulette (body language per the note in data/budCues.js; a small wrist-watch disc on the left arm in buildLush for `watch`), play their sounds through playCue from utils/sound.js (MR-69), weight munchies (the snack bit) 3x between 11:30-13:30 and 22:00-01:00 and yawn (the stretch bit) 3x after 21:00 using the existing dayMood hour, and perform the welcomeBack cue (perk up + wave + 'pop') when the cue prop asks for it. Keep the idle machine's guarantees: never while dragging, talking, dozing, or mid-cue.
Accept: Foreman capture: window.__bud.emote('lookAround'|'scratch'|'hum'|'watch') each produce distinct frames; setHour(12) then observing 20 roulette picks yields munchies at least 6 times (dev handle exposes the roulette pick function so the count is measured, not eyeballed); lint 0 warnings.
Red proof: Set the noon munchies weight back to 1x -> the 20-pick count drops below 6 in the measured run (report the count both ways).

<!--row id=MR-65 tier=S status=done lane=F deps=MR-62,MR-64,MR-67 files=frontend/src/hooks/useBudWander.js,frontend/src/components/Assistant/BudMascot.jsx,frontend/src/hooks/useDraggable.js,frontend/src/__tests__/useBudWander.test.jsx,frontend/src/contexts/AssistantContext.jsx,frontend/src/components/SettingsPanel.jsx,frontend/src/__tests__/components/SettingsPanel.test.jsx commit=fe47a66-->
**MR-65 [S] Bud wanders: walks to what you are looking at, points at alerts, hops home, with a Stay put switch** Gap: Bud sits where the user dragged him (useDraggable position persisted as bud.position). Build hooks/useBudWander.js: given the current position, a target element selector and the viewport, it animates position (rAF, ease-in-out, ~380 px/s) to a spot beside the target that does not cover it (choose the side with room; keep 16 px inside the viewport), emitting cue walk while moving, land on arrival, point when the target is an alert, and walking home (the user's saved corner) after 12 s of nothing. Targets come from data-bud-anchor attributes: BudMascot looks for [data-bud-anchor="alert"] (the out-of-range alerts block in PlantDetail and the alerts tile on the dashboard already exist; adding the attribute to those two elements is allowed here as the ONLY out-of-footprint edit and must be named in the report), and for the focused form (form:focus event -> walk near it). Never wander while dragging, minimised, while a tip bubble is open, while the tour runs, or when the new AssistantContext key wanderEnabled (localStorage 'bud.wanderEnabled', default true) is false; SettingsPanel gets a 'Stay put' toggle (label 'Bud wanders the screen', description 'Off keeps him in his corner.'). Position persistence: wandering must NOT overwrite the user's saved corner (useDraggable onCommit only on a real drag; read useDraggable and add a `follow` position override if needed).
Accept: useBudWander.test (jsdom, fake timers, getBoundingClientRect stubbed): given a target rect on the right edge, the hook moves position left of it and never overlaps the rect; after 12 s idle it returns to the home position; wanderEnabled=false yields no movement; a drag in progress yields no movement. SettingsPanel.test: toggling 'Bud wanders the screen' calls setWanderEnabled(false). The e2e suite (foreman runs it) stays green with Bud minimised as before.
Red proof: Remove the overlap check -> the never-overlaps test fails. Ignore wanderEnabled -> the no-movement test fails.

<!--row id=MR-66 tier=S status=done lane=F deps=MR-62,MR-67 files=frontend/src/data/budChat.js,frontend/src/components/Assistant/SpeechBubble.jsx,frontend/src/components/Assistant/AskMenu.jsx,frontend/src/components/Assistant/BudMascot.jsx,frontend/src/__tests__/budChat.test.js,frontend/src/__tests__/components/BudChat.test.jsx commit=02d7cce-->
**MR-66 [S] Talk back: type a question to Bud and get an offline, data-grounded, Towelie-flavoured answer** Gap: Bud answers four fixed questions from the ask menu (answerQuestion keys status/week/action/fun); there is no free-text input. Build data/budChat.js: answer(text, input, {voice, now}) -> {text, expression, cue} using ordered intent patterns over the snapshot (ph|acid -> latest pH vs band; ec|ppm|feed|nutrient -> EC/feeding due; height|tall|grow -> growth rate and total; harvest|when|ready -> harvestCountdown range; water|reservoir|change -> reservoir age; humid|temp|vpd -> latest readings; badge|streak -> earned badges; hello|hi|hey -> greeting; high|smoke|joint -> a Towelie deflection that still ends on a useful reminder; anything else -> the shrug cue plus 'I have no idea what's going on... but your pH was X' style fallback that still cites one real fact). Compose through budVoice (MR-67) so the clean voice works too. UI: the AskMenu gains a text input (aria-label 'Ask Bud anything', Enter submits) above the fixed items; BudMascot routes the answer into the SpeechBubble and fires the returned cue; answers are exempt from the 60 s rate limit (the user asked).
Accept: budChat.test: 'what's my ph' with a snapshot whose latest pH is 7.1 (band 5.8-6.2) returns text containing '7.1' and '5.8' and expression 'alert'; 'when can I harvest' with a late-flowering plant returns the range 'N-M'; gibberish returns cue 'shrug' and still contains one number from the snapshot; voice 'clean' contains no catchphrase. BudChat.test: typing in the input and pressing Enter renders the answer text in the bubble.
Red proof: Make the fallback drop the fact -> the gibberish test fails on the number. Remove the Enter handler -> the component test fails.

<!--row id=MR-70 tier=S status=done lane=D deps=MR-65,MR-68,MR-66,MR-63 files=tools/bud_capture.mjs,tools/README-bud-capture.md commit=4cdfcf7-->
**MR-70 [S] Bud capture strip: a Playwright script that drives every cue in the dev app and saves a contact sheet** Gap: No instrument can show what Bud's body actually does (jsdom has no WebGL, BudThree is untested by design). Build tools/bud_capture.mjs: starts the Vite dev server (npm run frontend:dev on a free port) with a seeded backend (createServer from server.js on a temp dir, token passed via VITE_API_BASE_URL/VITE_API_TOKEN as api.js supports), opens Chromium via Playwright, enables effects, waits for window.__bud, and for each cue name in data/budCues.js plus each idle emote: calls __bud.cue(name) / __bud.emote(name), captures 4 frames at 0, 30, 60, 90 percent of the cue's dur, and writes frames into one PNG contact sheet per cue under a chosen output dir plus an index.html; also captures a rest frame first and prints the per-cue pixel-diff against rest (the number the MR-64 red proof reads). Tears everything down.
Accept: node tools/bud_capture.mjs --out <dir> exits 0, writes one sheet per cue (>= 20 sheets), prints a table of cue name -> max pixel diff vs rest, and every reaction cue's diff is > 0.5 percent of pixels; README documents the run.
Red proof: Run with the cue machine disabled in the rig (dev handle __bud.disableCues()) -> every reaction cue's diff prints 0 and the script exits non-zero.

<!--row id=MR-71 tier=S status=done lane=E deps=MR-70 files=docs/user-guide.md,CHANGELOG.md,package.json,frontend/package.json,README.md commit=c53a24b-->
**MR-71 [S] Docs and version 1.4.0 for the Bud release** Gap: docs/user-guide.md describes Bud as a tip mascot; nothing covers the voice switch, Stay put, asking him questions, or what his body language means. Add a 'Bud' section (voice switch, wandering and Stay put, ask him anything, what nod/wince/sulk mean, sounds), bump both package.json to 1.4.0, regenerate the changelog section with python tools/changelog_from_board.py 1.4.0 <date> CHANGELOG.md (the third argument skips released rows), update README's Bud bullet.
Accept: both package.json read 1.4.0; the 1.4.0 changelog section lists MR-62..MR-70 and none of the 1.3.0 rows; the user guide gains >= 5 new '### ' headings; board lint green.
Red proof: Omit the CHANGELOG.md argument from the generator -> the 1.3.0 rows reappear (assert the count of '(MR-3' lines is 0 with the argument and > 0 without).

<!--row id=MR-72 tier=S status=done lane=F files=frontend/src/components/Assistant/lush/buildLush.js commit=0647f6a-->
**MR-72 [S] Tune the cheer and shrug arm poses so the raised arms stay in front of the leaf** Gap: the MR-70 contact sheets (cheer.png, shrug.png) show both arms swinging behind the leaf body at the peak of cheer and shrug, so the pose reads as arms missing rather than arms up; ARM_CHEER and ARM_SHRUG in buildLush.js were tuned by eye without the sheet. Owner verdict on the look (escalation class 1).
Accept: node tools/bud_capture.mjs --out <dir> then open cheer.png and shrug.png: at the 30 and 60 percent frames both arms are visible in front of or beside the leaf; the owner says it reads as a cheer.
Red proof: Revert ARM_CHEER to the current values and re-capture: the 60 percent frame shows no arms.

<!--row id=MR-73 tier=S status=done lane=D files=package.json,package-lock.json,.github/workflows/ci.yml commit=b62bf5d-->
**MR-73 [S] CI Linux jobs cannot install: the Windows-only rolldown binding must be optional** Gap (CI run 33745516729 on main, 2026-09-03): both test jobs fail in 10 s with npm error EBADPLATFORM: @rolldown/binding-win32-x64-msvc@1.2.7 wanted os win32, actual linux. The binding was added to root devDependencies in mr-run1 as a Windows workaround for the vite 8 pull; npm treats an unsupported devDependency as fatal but SKIPS an unsupported optionalDependency. Move it to optionalDependencies.
Accept: On main the CI test matrix (America/Los_Angeles and Pacific/Auckland) reaches the test steps and passes; locally npm install still resolves the binding on Windows (frontend build works).
Red proof: Move it back to devDependencies -> the Linux jobs fail at install again (the CI run is the instrument; cite the run id both ways).

<!--row id=MR-74 tier=O status=done lane=D files=e2e/a11y.spec.mjs,e2e/helpers.mjs,frontend/src/** commit=b448dde-->
**MR-74 [O] Axe reports serious violations on every tab on the CI Windows runner but not locally** Gap (CI run 33745516729 package-and-e2e job, 2026-09-03): all six a11y specs fail with 'axe violations on <tab>' while the same suite passes 20/20 on the owner's machine three times today. Same Electron, same spec; the difference is the runner (display scaling, colour scheme, fonts, or a timing where the page is captured before theme tokens apply). Download the error-context artefacts, read the violation ids, and fix the product or the staging, never the assertion.
Accept: The package-and-e2e job on main is green; the error-context names the violation ids and the fix commit explains which runner difference produced them.
Red proof: Revert the fix on a scratch branch and re-run the workflow -> the six specs fail again.

<!--row id=MR-75 tier=S status=done lane=D files=e2e/a11y.spec.mjs,e2e/helpers.mjs commit=d19d135-->
**MR-75 [S] Run the axe accessibility specs under BOTH colour schemes** Gap (CI run 33795688347, 2026-09-03): the a11y specs passed 6/6 on the owner's machine (dark OS theme) three times today and failed 6/6 on the CI runner (light theme) on two different elements in a row: the sidebar sub-line, then the active tab button whose class string lacked a space before text-white. One scheme per machine means half the palette is never checked. launchApp already sets localStorage keys before reload; the theme is a per-device preference in ThemeContext (read it for the key), so the spec can run each tab under 'light' and 'dark' explicitly instead of inheriting the OS.
Accept: npm run test:e2e runs the six tab checks twice (light and dark, named in the spec title: 12 a11y specs), both green locally; the CI package-and-e2e job is green.
Red proof: Re-introduce the missing space in App.jsx's active tab class on a scratch run -> the LIGHT variant fails while dark still passes (assert the precondition that the local OS theme is dark, or force it).
